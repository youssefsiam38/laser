/**
 * The fleet: the permanent column of agent work, and its sub-desktop sheet
 * (docs/ux-fleet.md). Both render the same panel.
 */
export { FleetPanel, type FleetPanelProps } from "./FleetPanel.js";
export { FleetSheet } from "./FleetSheet.js";
