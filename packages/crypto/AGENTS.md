# @lasercode/crypto

Noise IK/KK over 25519_AESGCM_SHA256 (WebCrypto first, `@noble` as fallback),
device pairing and the signed device list.

Runs unchanged in Node and in the browser. Keys never leave the endpoints.
