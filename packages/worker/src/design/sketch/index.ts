/**
 * Sketch grounding (M21-T13): the one door out of the sandboxed frame.
 *
 * Parse-only like everything else under `design/`: the document is text, and
 * nothing in here executes, fetches or evaluates any part of it.
 */
export * from "./ground.js";
