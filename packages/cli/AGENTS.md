# @lasercode/cli

The command-line entry point: host lifecycle, session verbs, doctor.

Speaks `@lasercode/protocol`; never imports Pi. It is a way into the same
product, not a second product with its own vocabulary.
