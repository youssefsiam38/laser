# @lasercode/pi-goal

A reusable, Pi-native durable session goal capability: the exact upstream pin,
the loader entrypoint, and a stable reader for goal state.

Useful to a native Pi user on its own — that is the point. Laser-specific
presentation belongs in the UI, not here.
