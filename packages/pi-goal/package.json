{
  "name": "@lasercode/pi-goal",
  "version": "0.2.3",
  "private": false,
  "type": "module",
  "description": "Reusable Pi-native durable session goal capability.",
  "license": "Apache-2.0",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@narumitw/pi-goal": "0.54.4"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "0.85.0"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.85.0"
  }
}
