# The bundled Node runtime: ~120 MB per platform, fetched by
# `pnpm -F @piorbit/desktop runtime` and pinned in runtime.json.
runtime/

# electron-builder output and its staging directories.
out/
build/runtime/

# Generated by `pnpm -F @piorbit/desktop icons` from scripts/make-icons.mjs.
# src/assets/icons.generated.ts is committed on purpose — it is source the tray
# imports — but the image files are not.
build/icon.png
build/icons/
