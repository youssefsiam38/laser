#!/usr/bin/env node
import { HOST_BIND_ADDRESS, HOST_DEFAULT_PORT } from "./index.js";

// TODO(M1-T1): start WebSocket server on HOST_BIND_ADDRESS:HOST_DEFAULT_PORT, serve the UI bundle, route to workers.
console.log(`piorbit host scaffold: would listen on ${HOST_BIND_ADDRESS}:${HOST_DEFAULT_PORT}`);
