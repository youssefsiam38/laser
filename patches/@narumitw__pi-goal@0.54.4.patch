diff --git a/dist/chunks/chunk-QWIUWKRW.js b/dist/chunks/chunk-QWIUWKRW.js
index 337f7dc01226f5d5a760a1a8ee1e75e0af44c9e6..ec21139433f19aa27f72462c3b5bd6471f3c630e 100644
--- a/dist/chunks/chunk-QWIUWKRW.js
+++ b/dist/chunks/chunk-QWIUWKRW.js
@@ -27,73 +27,35 @@ var REMOVED_QUEUE_COMMANDS = /* @__PURE__ */ new Set([
   "pop",
   "shift"
 ]);
-var TOKEN_BUDGET_COMPLETION = {
-  value: "--tokens ",
-  label: "--tokens",
-  description: "Set a token budget before the goal"
-};
 var GOAL_ARGUMENT_COMPLETIONS = [
   { value: "pause", label: "pause", description: "Pause the active goal" },
-  { value: "resume", label: "resume", description: "Resume a stopped or budget-limited goal" },
-  { value: "clear", label: "clear", description: "Clear the current goal" },
-  { value: "edit", label: "edit", description: "Edit the current goal objective" },
-  { value: "status", label: "status", description: "Show the current goal" },
-  TOKEN_BUDGET_COMPLETION
+  { value: "resume", label: "resume", description: "Resume the goal" },
+  { value: "clear", label: "clear", description: "Clear the active goal" },
+  { value: "edit", label: "edit", description: "Edit the objective" },
+  { value: "status", label: "status", description: "Show the current goal" }
 ];
 function completeGoalArguments(argumentPrefix) {
-  const prefix = argumentPrefix.trimStart();
-  if (prefix === "") return [...GOAL_ARGUMENT_COMPLETIONS];
-  const objectiveOption = /^edit\s+(\S*)$/.exec(prefix);
-  if (objectiveOption) {
-    const optionPrefix = objectiveOption[1] ?? "";
-    return optionPrefix === "" || "--tokens".startsWith(optionPrefix) ? [
-      {
-        value: "edit --tokens ",
-        label: "--tokens",
-        description: "Set a token budget before the updated goal"
-      }
-    ] : null;
-  }
-  if (/\s/.test(prefix)) return null;
-  const matches = GOAL_ARGUMENT_COMPLETIONS.filter(
-    (item) => item.value.startsWith(prefix) || item.label.startsWith(prefix)
-  );
-  return matches.length > 0 ? matches : null;
+  return GOAL_ARGUMENT_COMPLETIONS.filter(item => item.value.startsWith(argumentPrefix.trimStart()));
 }
+// The objective is task data, not shell syntax. Preserve quotes and newlines.
 function parseCommand(args) {
-  const tokens = tokenize(args.trim());
-  if (tokens.length === 0) return { kind: "show" };
-  const [first, ...rest] = tokens;
-  if (first === "pause") return rest.length === 0 ? { kind: "pause" } : "Usage: /goal pause";
-  if (first === "resume") return rest.length === 0 ? { kind: "resume" } : "Usage: /goal resume";
-  if (first === "clear" || first === "stop")
-    return rest.length === 0 ? { kind: "clear" } : "Usage: /goal clear";
-  if (first === "status") return rest.length === 0 ? { kind: "show" } : "Usage: /goal status";
-  if (first === "edit") return parseObjective("edit", rest);
-  return parseObjective("start", tokens);
+  const text = args.trim();
+  if (!text) return { kind: "show" };
+  const first = /^\S+/.exec(text)[0];
+  const rest = text.slice(first.length).trim();
+  if (["pause", "resume", "clear", "stop", "status"].includes(first)) {
+    if (rest) return "Usage: /goal " + first;
+    return { kind: first === "stop" ? "clear" : first === "status" ? "show" : first };
+  }
+  return parseObjective(first === "edit" ? "edit" : "start", first === "edit" ? rest : text);
 }
 function isRemovedQueueCommand(args) {
-  const [first] = tokenize(args.trim());
-  return first !== void 0 && REMOVED_QUEUE_COMMANDS.has(first);
-}
-function parseObjective(kind, tokens) {
-  let tokenBudget;
-  const objectiveTokens = [...tokens];
-  if (objectiveTokens[0] === "--tokens") {
-    const rawBudget = objectiveTokens[1];
-    if (!rawBudget) {
-      return kind === "start" ? "Usage: /goal --tokens 100k <goal_to_complete>" : `Usage: /goal ${kind} --tokens 100k <goal_to_complete>`;
-    }
-    const parsedBudget = parseTokenBudget(rawBudget);
-    if (parsedBudget === void 0) return `Invalid token budget: ${rawBudget}`;
-    tokenBudget = parsedBudget;
-    objectiveTokens.splice(0, 2);
-  }
-  if (objectiveTokens.length === 0) {
-    if (kind === "start") return "Usage: /goal <goal_to_complete>";
-    return `Usage: /goal ${kind} <goal_to_complete>`;
-  }
-  return { kind, objective: objectiveTokens.join(" "), tokenBudget };
+  return REMOVED_QUEUE_COMMANDS.has(args.trim().split(/\s/)[0]);
+}
+function parseObjective(kind, objective) {
+  if (/^--tokens(?:\s|$)/.test(objective)) return "Goals do not have token budgets. Use /goal <objective>.";
+  if (!objective) return "Usage: /goal " + (kind === "edit" ? "edit " : "") + "<objective>";
+  return { kind, objective };
 }
 function tokenize(input) {
   const tokens = [];
@@ -509,11 +471,12 @@ function normalizeLoadedGoal(goal) {
     startedAt: isNonNegativeFiniteNumber(goal.startedAt) ? goal.startedAt : now,
     updatedAt: isNonNegativeFiniteNumber(goal.updatedAt) ? goal.updatedAt : now,
     iteration: Math.max(0, Math.floor(nonNegativeFiniteNumber(goal.iteration))),
-    tokenBudget: normalizeTokenBudget(goal.tokenBudget),
-    tokensUsed: nonNegativeFiniteNumber(goal.tokensUsed),
-    timeUsedSeconds: nonNegativeFiniteNumber(goal.timeUsedSeconds),
-    baselineTokens: nonNegativeFiniteNumber(goal.baselineTokens),
-    activeStartedAt: goal.status === "active" && !waiting ? now : void 0,
+    tokenBudget: void 0,
+    status: goal.status === "budget_limited" ? "paused" : goal.status,
+    tokensUsed: 0,
+    timeUsedSeconds: 0,
+    baselineTokens: 0,
+    activeStartedAt: void 0,
     automaticModelTurns: normalizeSafetyCounter(goal.automaticModelTurns),
     toolFreeRepeatCount: normalizeSafetyCounter(goal.toolFreeRepeatCount),
     lastToolFreeOutputFingerprint: normalizeOutputFingerprint(goal.lastToolFreeOutputFingerprint),
@@ -639,16 +602,49 @@ function isRecord3(value) {
 var GOAL_COMPLETE_TOOL = "goal_complete";
 var GOAL_BLOCKED_TOOL = "goal_blocked";
 var GOAL_WAIT_TOOL = "goal_wait";
-var REQUIRED_GOAL_TOOL_NAMES = [GOAL_COMPLETE_TOOL, GOAL_BLOCKED_TOOL];
+var REQUIRED_GOAL_TOOL_NAMES = [GOAL_COMPLETE_TOOL, GOAL_BLOCKED_TOOL, GOAL_WAIT_TOOL];
+function ensureRequiredToolsActive(pi) {
+  let registered;
+  let active;
+  try {
+    registered = new Set(pi.getAllTools().map((tool) => tool.name));
+    active = new Set(pi.getActiveTools());
+  } catch {
+    return {
+      ok: false,
+      reason: "activation_refused",
+      message: "Goal tools could not be inspected. Reload this conversation; if the problem continues, turn Goals off and on in Features."
+    };
+  }
+  const missingRegistration = REQUIRED_GOAL_TOOL_NAMES.filter((name) => !registered.has(name));
+  if (missingRegistration.length > 0) {
+    return {
+      ok: false,
+      reason: "missing_registration",
+      message: "Goal tools did not load. Reload this conversation; if the problem continues, turn Goals off and on in Features."
+    };
+  }
+  const missingActive = REQUIRED_GOAL_TOOL_NAMES.filter((name) => !active.has(name));
+  if (missingActive.length === 0) return { ok: true };
+  try {
+    pi.setActiveTools([...active, ...missingActive]);
+    const repaired = new Set(pi.getActiveTools());
+    if (REQUIRED_GOAL_TOOL_NAMES.every((name) => repaired.has(name))) return { ok: true };
+  } catch {
+  }
+  return {
+    ok: false,
+    reason: "activation_refused",
+    message: "Goal tools could not be activated. Reload this conversation; if the problem continues, turn Goals off and on in Features."
+  };
+}
 function goalToolsAvailable(pi) {
-  const active = new Set(pi.getActiveTools());
-  return REQUIRED_GOAL_TOOL_NAMES.every((name) => active.has(name));
+  return ensureRequiredToolsActive(pi).ok;
 }
 function assertGoalToolsAvailable(pi) {
-  if (goalToolsAvailable(pi)) return;
-  throw new Error(
-    "goal_complete and goal_blocked are unavailable; include them in the active tool allowlist or leave the restrictive tool mode first."
-  );
+  const result = ensureRequiredToolsActive(pi);
+  if (result.ok) return;
+  throw new Error(result.message);
 }
 
 // src/workflow-mutex.ts
@@ -657,6 +653,7 @@ var AGENT_WORKFLOW_GROUP = "agent-workflow";
 var WorkflowMutex = class {
   session;
   heldGroups = /* @__PURE__ */ new Map();
+  releaseWaiters = /* @__PURE__ */ new Set();
   generation = 0;
   pi;
   constructor(pi) {
@@ -666,14 +663,16 @@ var WorkflowMutex = class {
     });
   }
   bindSession(session) {
+    this.cancelReleaseWaiters();
+    this.releaseAll();
     this.generation += 1;
-    this.heldGroups.clear();
     this.session = session;
   }
   unbindSession(session) {
     if (this.session !== session) return;
+    this.cancelReleaseWaiters();
+    this.releaseAll();
     this.generation += 1;
-    this.heldGroups.clear();
     this.session = void 0;
   }
   acquire(group = AGENT_WORKFLOW_GROUP) {
@@ -698,12 +697,49 @@ var WorkflowMutex = class {
   }
   release(owner, group = AGENT_WORKFLOW_GROUP) {
     if (!this.isOwner(owner, group)) return;
+    const session = this.session;
     this.heldGroups.delete(group);
+    if (session) this.emitRelease(session, group);
+  }
+  waitForRelease(session, callback, group = AGENT_WORKFLOW_GROUP) {
+    const waiter = { session, group, callback };
+    this.releaseWaiters.add(waiter);
+    return () => {
+      this.releaseWaiters.delete(waiter);
+    };
+  }
+  cancelReleaseWaiters() {
+    this.releaseWaiters.clear();
+  }
+  releaseAll() {
+    const session = this.session;
+    if (!session) {
+      this.heldGroups.clear();
+      return;
+    }
+    for (const group of [...this.heldGroups.keys()]) {
+      this.heldGroups.delete(group);
+      this.emitRelease(session, group);
+    }
+  }
+  emitRelease(session, group) {
+    try {
+      this.pi.events.emit(WORKFLOW_MUTEX_CHANNEL, { session, group, released: true });
+    } catch {
+    }
   }
   answer(payload) {
     try {
       if (typeof payload !== "object" || payload === null || Array.isArray(payload)) return;
       const attempt = payload;
+      if (attempt.released === true) {
+        for (const waiter of [...this.releaseWaiters]) {
+          if (waiter.session !== attempt.session || waiter.group !== attempt.group) continue;
+          this.releaseWaiters.delete(waiter);
+          waiter.callback();
+        }
+        return;
+      }
       if (attempt.session !== this.session) return;
       if (attempt.group !== AGENT_WORKFLOW_GROUP) return;
       if (typeof attempt.busy !== "boolean") return;
@@ -717,6 +753,7 @@ var WorkflowMutex = class {
 // src/runtime.ts
 var STATUS_KEY = "goal";
 var GOAL_STATE_ENTRY_TYPE2 = "goal-state";
+var GOAL_TRANSITION_ENTRY_TYPE = "goal-transition";
 var MAX_GOAL_ID_LENGTH = 128;
 function isTerminalGoalStatus(status) {
   return status !== "active" && status !== "queued";
@@ -765,6 +802,8 @@ var GoalRuntime = class {
   workflowMutex;
   workflowOwner;
   workflowSession;
+  pendingWorkflowRestore;
+  lastGoalToolFailure;
   pendingGoalPromptMarkers = /* @__PURE__ */ new Map();
   claimedGoalPromptMarkers = /* @__PURE__ */ new Map();
   cancelledGoalPromptMarkers = /* @__PURE__ */ new Map();
@@ -778,19 +817,28 @@ var GoalRuntime = class {
     this.pi = pi;
     this.workflowMutex = new WorkflowMutex(pi);
   }
+  ensureGoalToolsActive() {
+    const result = ensureRequiredToolsActive(this.pi);
+    this.lastGoalToolFailure = result.ok ? void 0 : result;
+    return result.ok;
+  }
   goalToolsAvailable() {
-    return goalToolsAvailable(this.pi);
+    return this.ensureGoalToolsActive();
   }
   assertGoalToolsAvailable() {
-    assertGoalToolsAvailable(this.pi);
+    const result = ensureRequiredToolsActive(this.pi);
+    this.lastGoalToolFailure = result.ok ? void 0 : result;
+    if (!result.ok) throw new Error(result.message);
   }
   bindWorkflowSession(session) {
+    this.cancelPendingWorkflowRestore();
     this.workflowSession = session;
     this.workflowOwner = void 0;
     this.workflowMutex.bindSession(session);
   }
   unbindWorkflowSession(session) {
     if (this.workflowSession !== session) return;
+    this.cancelPendingWorkflowRestore();
     this.workflowMutex.unbindSession(session);
     this.workflowOwner = void 0;
     this.workflowSession = void 0;
@@ -813,6 +861,65 @@ var GoalRuntime = class {
     this.workflowMutex.release(owner);
     if (!this.workflowMutex.isOwner(owner)) this.workflowOwner = void 0;
   }
+  deferWorkflowRestore(ctx, goalId, retry) {
+    this.cancelPendingWorkflowRestore();
+    const pending = {
+      session: ctx.sessionManager,
+      goalId,
+      generation: this.menuGeneration,
+      retry,
+      unsubscribe: void 0
+    };
+    this.pendingWorkflowRestore = pending;
+    this.armPendingWorkflowRestore(pending);
+  }
+  armPendingWorkflowRestore(pending = this.pendingWorkflowRestore) {
+    if (!pending || this.pendingWorkflowRestore !== pending || pending.unsubscribe) return;
+    pending.unsubscribe = this.workflowMutex.waitForRelease(pending.session, () => {
+      if (this.pendingWorkflowRestore !== pending) return;
+      pending.unsubscribe = void 0;
+      pending.retry();
+    });
+  }
+  hasPendingWorkflowRestore() {
+    return this.pendingWorkflowRestore !== void 0;
+  }
+  pendingWorkflowRestoreMatches(ctx, goalId) {
+    const pending = this.pendingWorkflowRestore;
+    return Boolean(pending && pending.session === ctx.sessionManager && pending.goalId === goalId && pending.generation === this.menuGeneration);
+  }
+  retryPendingWorkflowRestore() {
+    this.pendingWorkflowRestore?.retry();
+  }
+  completePendingWorkflowRestore() {
+    const pending = this.pendingWorkflowRestore;
+    if (!pending) return;
+    pending.unsubscribe?.();
+    this.pendingWorkflowRestore = void 0;
+  }
+  cancelPendingWorkflowRestore() {
+    const pending = this.pendingWorkflowRestore;
+    if (!pending) return;
+    pending.unsubscribe?.();
+    this.pendingWorkflowRestore = void 0;
+  }
+  reportUnavailableGoalTools(ctx, abortTurn = true, recordUsage = true) {
+    const goal = this.activeGoal;
+    if (goal?.status !== "active") return false;
+    if (recordUsage) this.recordGoalUsage(goal, ctx);
+    this.cancelContinuationWork();
+    this.clearGoalRecoveryForGoal(goal.id);
+    this.clearBudgetWrapUp();
+    this.persistGoal(goal);
+    this.updateStatus(ctx, goal);
+    if (abortTurn) abortCurrentTurn(ctx);
+    notifyTerminal(
+      ctx.ui,
+      this.lastGoalToolFailure?.message ?? "Goal tools are unavailable. Reload this conversation; if the problem continues, turn Goals off and on in Features.",
+      "error"
+    );
+    return true;
+  }
   hasLegacyQueueInterface() {
     return this.legacyExperimentalGoalsSetting || this.legacyQueueState !== void 0;
   }
@@ -901,8 +1008,8 @@ var GoalRuntime = class {
       return false;
     }
     if (!intent) return false;
-    if (this.activeGoal?.status === "active" && !this.goalToolsAvailable()) {
-      this.pauseGoalForUnavailableTools(ctx);
+    if (this.activeGoal?.status === "active" && !this.ensureGoalToolsActive()) {
+      this.reportUnavailableGoalTools(ctx, false);
       return false;
     }
     if (!this.activeGoal || this.activeGoal.id !== intent.goalId || this.activeGoal.status !== "active" || this.activeGoal.waiting) {
@@ -1047,9 +1154,24 @@ var GoalRuntime = class {
       formatStatus(goal, this.settings.continuationLimits.automaticTurns)
     );
   }
+  recordGoalTransition(goal, transition) {
+    this.pi.appendEntry(GOAL_TRANSITION_ENTRY_TYPE, {
+      goalId: goal.id,
+      at: Date.now(),
+      cause: transition.cause,
+      initiator: transition.initiator,
+      previousStatus: transition.previousStatus,
+      status: transition.status,
+      ...transition.reason ? { reason: transition.reason } : {},
+      ...transition.abortReason ? { abortReason: transition.abortReason } : {},
+      invocationId: transition.invocationId ?? `goal:${goal.id}:${goal.iteration}`,
+      ...transition.runId ? { runId: transition.runId } : {}
+    });
+  }
   stopActiveGoal(ctx, request) {
     const currentGoal = this.activeGoal;
     if (!currentGoal || currentGoal.id !== request.expectedGoalId) return void 0;
+    this.cancelPendingWorkflowRestore();
     this.clearGoalWaitTimer();
     let goal = currentGoal;
     let status;
@@ -1135,6 +1257,24 @@ var GoalRuntime = class {
     if (terminalReason !== void 0) this.setTerminalReason(this.activeGoal.id, terminalReason);
     const stoppedGoal = this.activeGoal;
     this.persistGoal(stoppedGoal);
+    if (request.kind !== "blocker_report") {
+      const defaults = {
+        explicit_pause: { cause: "pause_action", initiator: "person" },
+        budget_limit: { cause: "budget_limit", initiator: "engine" },
+        safety_pause: { cause: request.cause, initiator: "engine" },
+        retry_exhausted: { cause: "provider_retry_exhausted", initiator: "failure" },
+        tools_unavailable: { cause: "goal_tools_unavailable", initiator: "engine" },
+        agent_interruption: { cause: "provider_failure", initiator: "failure" },
+        activation_rollback: { cause: "activation_rollback", initiator: "engine" }
+      };
+      this.recordGoalTransition(stoppedGoal, {
+        ...defaults[request.kind],
+        ...request.transition,
+        previousStatus: currentGoal.status,
+        status: stoppedGoal.status,
+        reason: request.transition?.reason ?? terminalReason
+      });
+    }
     if (request.kind !== "blocker_report") this.ensureInactiveGoalContextContract(ctx);
     if (this.activeGoal?.id === stoppedGoal.id && this.activeGoal.status === stoppedGoal.status) {
       this.updateStatus(ctx, stoppedGoal);
@@ -1265,12 +1405,12 @@ var GoalRuntime = class {
       expectedGoalId: goal.id,
       cause,
       abortTurn,
-      reason: `${cause} (${count}; ${formatTokenCount(goal.tokensUsed)} tokens)`
+      reason: `${cause} (${count})`
     });
     if (!stoppedGoal) return false;
     notifyTerminal(
       ctx.ui,
-      cause === "continuation_limit" ? `Automatic-work limit reached: ${stoppedGoal.automaticModelTurns} of ${automaticLimit} responses. Goal progress is saved with ${formatTokenCount(stoppedGoal.tokensUsed)} cumulative tokens. Open /goal to review and continue.` : `Goal paused: ${count}; ${formatTokenCount(stoppedGoal.tokensUsed)} cumulative tokens. Open /goal to review and continue.`,
+      cause === "continuation_limit" ? `Automatic-work limit reached: ${stoppedGoal.automaticModelTurns} of ${automaticLimit} responses. Goal progress is saved. Open /goal to review and continue.` : `Goal paused: ${count}. Open /goal to review and continue.`,
       "warning"
     );
     return true;
@@ -1556,15 +1696,20 @@ var GoalRuntime = class {
     this.clearTerminalDetails();
     clearLegacyPersistedGoal(cwd);
   }
-  clearActiveGoal(ctx, reason = "goal cleared", releaseWorkflow = true) {
-    this.clearActiveGoalState(ctx, reason, releaseWorkflow);
+  clearActiveGoal(ctx, reason = "goal cleared", releaseWorkflow = true, transition = {
+    cause: "clear_action",
+    initiator: "person",
+    status: "cleared"
+  }) {
+    this.clearActiveGoalState(ctx, reason, releaseWorkflow, transition);
     this.ensureInactiveGoalContextContract(ctx);
   }
   clearCompletedGoal(ctx) {
     this.clearActiveGoalState(ctx, "goal cleared", true);
   }
-  clearActiveGoalState(ctx, reason, releaseWorkflow) {
+  clearActiveGoalState(ctx, reason, releaseWorkflow, transition) {
     const clearedGoal = this.activeGoal;
+    this.cancelPendingWorkflowRestore();
     this.clearGoalWaitTimer();
     this.cancelContinuationWork();
     this.clearGoalRecovery();
@@ -1573,6 +1718,13 @@ var GoalRuntime = class {
     this.activeGoal = void 0;
     this.legacyQueueState = void 0;
     this.clearPersistedGoal(ctx.cwd, clearedGoal, reason);
+    if (clearedGoal && transition) {
+      this.recordGoalTransition(clearedGoal, {
+        ...transition,
+        previousStatus: clearedGoal.status,
+        reason
+      });
+    }
     ctx.ui.setStatus(STATUS_KEY, void 0);
     if (releaseWorkflow) this.releaseWorkflow();
   }
@@ -1609,23 +1761,6 @@ var GoalRuntime = class {
     this.cancelledContinuationMarkers = new Map(snapshot.cancelledContinuationMarkers);
     this.terminalDetails = snapshot.terminalDetails ? structuredClone(snapshot.terminalDetails) : void 0;
   }
-  pauseGoalForUnavailableTools(ctx, abortTurn = true, recordUsage = true) {
-    const goal = this.activeGoal;
-    if (goal?.status !== "active") return false;
-    const stoppedGoal = this.stopActiveGoal(ctx, {
-      kind: "tools_unavailable",
-      expectedGoalId: goal.id,
-      abortTurn,
-      recordUsage
-    });
-    if (!stoppedGoal) return false;
-    notifyTerminal(
-      ctx.ui,
-      "Goal tools are unavailable, so the active goal was paused. Restore the tools and run /goal resume.",
-      "warning"
-    );
-    return true;
-  }
   showCompletionStatus(ctx) {
     this.clearCompletionStatusTimer();
     ctx.ui.setStatus(STATUS_KEY, "complete");
@@ -1705,11 +1840,11 @@ function createGoal(text, tokenBudget, baselineTokens) {
     startedAt: now,
     updatedAt: now,
     iteration: 0,
-    tokenBudget,
+    tokenBudget: void 0,
     tokensUsed: 0,
     timeUsedSeconds: 0,
-    baselineTokens,
-    activeStartedAt: now,
+    baselineTokens: 0,
+    activeStartedAt: void 0,
     automaticModelTurns: 0,
     toolFreeRepeatCount: 0
   };
@@ -1758,7 +1893,7 @@ function formatStatus(goal, automaticTurnLimit = DEFAULT_GOAL_SETTINGS.continuat
   if (goal.status === "usage_limited") return `usage \xB7 ${automatic}`;
   if (goal.status === "budget_limited") return `budget ${formatBudget2(goal)} \xB7 ${automatic}`;
   if (goal.tokenBudget !== void 0) return `active ${formatBudget2(goal)} \xB7 ${automatic}`;
-  return `active ${formatDuration(goal.timeUsedSeconds)} \xB7 ${automatic}`;
+  return `active \xB7 ${automatic}`;
 }
 function formatBudget2(goal) {
   return `${formatTokenCount(goal.tokensUsed)}/${formatTokenCount(goal.tokenBudget ?? 0)}`;
@@ -1773,8 +1908,7 @@ function goalSummary(goal, automaticTurnLimit = DEFAULT_GOAL_SETTINGS.continuati
     ] : [],
     `Iteration: ${goal.iteration}`,
     automaticTurnLimit === null ? `Automatic work: ${goal.automaticModelTurns} responses \xB7 Unlimited` : `Automatic work: ${goal.automaticModelTurns} of ${automaticTurnLimit} responses`,
-    `Active elapsed: ${formatDuration(goal.timeUsedSeconds)}`,
-    `Tokens: ${goal.tokenBudget === void 0 ? formatTokenCount(goal.tokensUsed) : formatBudget2(goal)}`
+    "Usage is tracked by the session, not the goal."
   ];
   if (goal.safetyPauseCause) {
     summary.push(
diff --git a/dist/chunks/chunk-UQRRW4CY.js b/dist/chunks/chunk-UQRRW4CY.js
index d3dccf8005f4552dd6d7994381245b74f0faeba3..ba67fdea9c643791717b606c4582534fccc6cfaf 100644
--- a/dist/chunks/chunk-UQRRW4CY.js
+++ b/dist/chunks/chunk-UQRRW4CY.js
@@ -3,22 +3,13 @@
 
 // src/accounting.ts
 function checkpointGoalActiveTime(goal, now, continueClock) {
-  const accumulated = nonNegativeFiniteNumber(goal.timeUsedSeconds);
-  const startedAt = goal.activeStartedAt;
-  if (typeof startedAt === "number" && Number.isFinite(startedAt)) {
-    goal.timeUsedSeconds = accumulated + Math.max(0, now - startedAt) / 1e3;
-  } else {
-    goal.timeUsedSeconds = accumulated;
-  }
-  goal.activeStartedAt = continueClock ? now : void 0;
+  // Session telemetry owns accounting. Neutral fields satisfy upstream's
+  // persistence validator without collecting a second set of usage metrics.
+  goal.timeUsedSeconds = 0;
+  goal.activeStartedAt = void 0;
 }
 function updateGoalUsage(goal, ctx, continueClock = goal.status === "active") {
-  const now = Date.now();
-  const baselineTokens = nonNegativeFiniteNumber(goal.baselineTokens);
-  goal.baselineTokens = baselineTokens;
-  goal.tokensUsed = Math.max(0, currentTokenTotal(ctx) - baselineTokens);
-  checkpointGoalActiveTime(goal, now, continueClock);
-  goal.updatedAt = now;
+  goal.updatedAt = Date.now();
 }
 function formatDuration(seconds) {
   const wholeSeconds = Math.max(0, Math.floor(nonNegativeFiniteNumber(seconds)));
@@ -65,8 +56,7 @@ function cumulativeAssistantTokens(entries) {
   return total;
 }
 function currentTokenTotal(ctx) {
-  const sessionManager = ctx.sessionManager;
-  return cumulativeAssistantTokens(sessionManager?.getBranch?.() ?? []);
+  return 0;
 }
 
 // src/errors.ts
@@ -203,7 +193,10 @@ import { getAgentDir } from "@earendil-works/pi-coding-agent";
 var GOAL_SETTINGS_FILE = "pi-goal.json";
 var DEFAULT_GOAL_SETTINGS = {
   rpc: { enabled: false },
-  continuationLimits: { automaticTurns: 25, noProgressTurns: 3 }
+  // The patched runtime never stops autonomous work because a counter expired.
+  // The counters remain persisted as diagnostics, and an explicit pi-goal
+  // settings file can still opt into an upstream limit.
+  continuationLimits: { automaticTurns: null, noProgressTurns: null }
 };
 function normalizeGoalSettings(value) {
   if (typeof value !== "object" || value === null || Array.isArray(value)) return void 0;
diff --git a/dist/index.ts b/dist/index.ts
index 88023ea7e232329a3cf4b26b23dd2078b837fb5d..d1f61c3f40a83aeaf89fb6957d09fe8062ef7b0b 100644
--- a/dist/index.ts
+++ b/dist/index.ts
@@ -69,7 +69,7 @@ function registerGoalCommand(pi, runtime, commands, options = {}) {
     options.loadGoalSettings ?? (() => import("./chunks/settings-ui-MVYTTBHW.js"))
   );
   pi.registerCommand("goal", {
-    description: "Run a goal to completion: /goal [--tokens 100k] <goal_to_complete>",
+    description: "Run a goal to completion: /goal <objective>",
     getArgumentCompletions: (prefix) => completeGoalArguments(prefix),
     handler: async (args, ctx) => {
       if (runtime.hasLegacyQueueInterface() && isRemovedQueueCommand(args)) {
@@ -81,35 +81,6 @@ function registerGoalCommand(pi, runtime, commands, options = {}) {
         reportCommandError(result, ctx);
         return;
       }
-      if (result.kind === "show" && args.trim() === "") {
-        const menuIsCurrent = captureMenuOwnership(runtime);
-        let managerModule;
-        try {
-          managerModule = await loadGoalManager();
-        } catch (error) {
-          if (!menuIsCurrent()) return;
-          throw error;
-        }
-        if (!menuIsCurrent()) return;
-        const { showGoalManager } = managerModule;
-        await showGoalManager(runtime, commands, ctx, async (menuCtx, target) => {
-          const settingsAreCurrent = captureMenuOwnership(runtime);
-          let settingsModule;
-          try {
-            settingsModule = await loadGoalSettings();
-          } catch (error) {
-            if (!settingsAreCurrent()) return;
-            throw error;
-          }
-          if (!settingsAreCurrent()) return;
-          const { showGoalSettings } = settingsModule;
-          await showGoalSettings(runtime, menuCtx, {
-            settingsPath: options.settingsPath,
-            initialScreen: target
-          });
-        });
-        return;
-      }
       switch (result.kind) {
         case "show":
           commands.showGoal(ctx);
@@ -198,6 +169,7 @@ New goal: ${safeGoalMenuText(objective, 4e3)}`
       }
     }
     if (isRequestCurrent && !isRequestCurrent()) return;
+    this.runtime.cancelPendingWorkflowRestore();
     const retainedOwner = this.runtime.ownsWorkflow(existingGoal);
     if (!this.runtime.acquireWorkflow(ctx.sessionManager)) return this.reportWorkflowBusy(ctx);
     const acquiredForRequest = !retainedOwner;
@@ -205,8 +177,7 @@ New goal: ${safeGoalMenuText(objective, 4e3)}`
       this.runtime.assertGoalToolsAvailable();
     } catch (error) {
       notifyTerminal(ctx.ui, `Cannot start /goal: ${formatError(error)}`, "error");
-      if (existingGoal?.status === "active") this.runtime.pauseGoalForUnavailableTools(ctx);
-      else if (acquiredForRequest) this.runtime.releaseWorkflow();
+      if (acquiredForRequest && existingGoal?.status !== "active") this.runtime.releaseWorkflow();
       return;
     }
     this.runtime.clearGoalWaitTimer();
@@ -273,7 +244,11 @@ New goal: ${safeGoalMenuText(objective, 4e3)}`
           this.runtime.clearStaleGoalToolCallBlock();
           ctx.ui.setStatus(STATUS_KEY, void 0);
         } else {
-          this.runtime.clearActiveGoal(ctx, "goal activation rolled back", false);
+          this.runtime.clearActiveGoal(ctx, "goal activation rolled back", false, {
+            cause: "activation_rollback",
+            initiator: "engine",
+            status: "cleared"
+          });
         }
       }
       if (rolledBackStartedGoal) {
@@ -286,6 +261,15 @@ New goal: ${safeGoalMenuText(objective, 4e3)}`
     if (this.runtime.activeGoal?.id !== startedGoal.id || !this.runtime.ownsWorkflow(this.runtime.activeGoal)) {
       return;
     }
+    if (existingGoal) {
+      this.runtime.recordGoalTransition(existingGoal, {
+        cause: "replace_action",
+        initiator: "person",
+        previousStatus: existingGoal.status,
+        status: "replaced",
+        reason: "Goal replaced by a new objective"
+      });
+    }
     const automaticLimit = this.runtime.settings.continuationLimits.automaticTurns;
     notifyTerminal(
       ctx.ui,
@@ -293,7 +277,7 @@ New goal: ${safeGoalMenuText(objective, 4e3)}`
       automaticLimit === null ? "warning" : "info"
     );
   }
-  pauseGoal(ctx) {
+  pauseGoal(ctx, transition) {
     if (!this.runtime.activeGoal) {
       notifyTerminal(ctx.ui, "No active goal.", "info");
       return;
@@ -308,7 +292,8 @@ New goal: ${safeGoalMenuText(objective, 4e3)}`
     }
     const stoppedGoal = this.runtime.stopActiveGoal(ctx, {
       kind: "explicit_pause",
-      expectedGoalId: this.runtime.activeGoal.id
+      expectedGoalId: this.runtime.activeGoal.id,
+      transition
     });
     if (stoppedGoal) notifyTerminal(ctx.ui, `Goal paused: ${stoppedGoal.text}`, "info");
   }
@@ -472,6 +457,7 @@ New goal: ${safeGoalMenuText(objective, 4e3)}`
     }
     const previousStatus = currentGoal.status;
     const intendsActive = editedGoalStatus(previousStatus) === "active";
+    this.runtime.cancelPendingWorkflowRestore();
     const retainedOwner = this.runtime.ownsWorkflow(currentGoal);
     if (intendsActive && !this.runtime.acquireWorkflow(ctx.sessionManager)) {
       this.reportWorkflowBusy(ctx);
@@ -483,8 +469,7 @@ New goal: ${safeGoalMenuText(objective, 4e3)}`
         this.runtime.assertGoalToolsAvailable();
       } catch (error) {
         notifyTerminal(ctx.ui, `Cannot reactivate /goal: ${formatError(error)}`, "error");
-        if (currentGoal.status === "active") this.runtime.pauseGoalForUnavailableTools(ctx);
-        else if (acquiredForEdit) this.runtime.releaseWorkflow();
+        if (acquiredForEdit && currentGoal.status !== "active") this.runtime.releaseWorkflow();
         return;
       }
     }
@@ -604,6 +589,52 @@ New goal: ${safeGoalMenuText(objective, 4e3)}`
 var REMOVED_QUEUE_SETTING_WARNING = "Ordered goal queue has been removed. Use /goal edit to reprioritize an active objective, or start /goal <objectives> if no active goal exists.";
 var REMOVED_PERSISTED_QUEUE_WARNING = "Ordered goal queue has been removed. Start /goal <objectives> to continue with one merged objective, or use /goal clear to discard the old queue state.";
 function registerGoalLifecycle(pi, runtime, runController, options = {}) {
+  const activateRestoredGoal = (ctx, goal2, continueAfterContention) => {
+    runtime.activeGoal = goal2;
+    if (runtime.activeGoal.safetyResetPending) {
+      runtime.activeGoal = resetGoalSafetyEpoch(runtime.activeGoal);
+    }
+    runtime.recordGoalUsage(runtime.activeGoal, ctx);
+    if (runtime.limitActiveGoalForBudget(ctx, false)) return false;
+    if (runtime.enforceAutomaticTurnLimit(ctx, false) || runtime.enforceNoProgressLimit(ctx)) {
+      return false;
+    }
+    if (!runtime.ensureGoalToolsActive()) {
+      runtime.reportUnavailableGoalTools(ctx, false, false);
+      return false;
+    }
+    runtime.persistGoal(runtime.activeGoal);
+    if (!runtime.ownsWorkflow(runtime.activeGoal)) return false;
+    const restoredGoalId = runtime.activeGoal.id;
+    runtime.ensureGoalContextContract(ctx, runtime.activeGoal);
+    if (runtime.activeGoal?.id !== restoredGoalId || runtime.activeGoal.status !== "active" || !runtime.ownsWorkflow(runtime.activeGoal)) {
+      return false;
+    }
+    runtime.updateStatus(ctx, runtime.activeGoal);
+    runtime.restoreGoalWaitTimer(ctx);
+    if (continueAfterContention && !runtime.activeGoal.waiting) {
+      runtime.requestContinuation(runtime.activeGoal);
+      if (!runtime.dispatchContinuationIfSettled(ctx)) {
+        runtime.scheduleContinuationDispatch(ctx, runtime.activeGoal.id);
+      }
+    }
+    return true;
+  };
+  const retryPendingRestore = (ctx) => {
+    if (!runtime.hasPendingWorkflowRestore()) return false;
+    const loaded = loadGoalStateFromSession(ctx);
+    const goal2 = loaded.goal;
+    if (!goal2 || goal2.status !== "active" || !runtime.pendingWorkflowRestoreMatches(ctx, goal2.id)) {
+      runtime.cancelPendingWorkflowRestore();
+      return false;
+    }
+    if (!runtime.acquireWorkflow(ctx.sessionManager)) {
+      runtime.armPendingWorkflowRestore();
+      return false;
+    }
+    runtime.completePendingWorkflowRestore();
+    return activateRestoredGoal(ctx, goal2, true);
+  };
   pi.on("session_start", async (_event, ctx) => {
     runtime.bindWorkflowSession(ctx.sessionManager);
     runtime.replaceMenuSession();
@@ -638,40 +669,14 @@ function registerGoalLifecycle(pi, runtime, runController, options = {}) {
       notifyTerminal(ctx.ui, REMOVED_QUEUE_SETTING_WARNING, "warning");
     }
     if (loaded.goal?.status === "active") {
-      if (!runtime.acquireWorkflow()) {
-        runtime.activeGoal = transitionGoal(loaded.goal, "paused");
-        runtime.persistGoal(runtime.activeGoal);
-        runtime.ensureInactiveGoalContextContract(ctx);
-        runtime.updateStatus(ctx, runtime.activeGoal);
-        notifyTerminal(
-          ctx.ui,
-          "Goal was paused during restore because another workflow is active in this session. Resume it after the other workflow ends.",
-          "warning"
-        );
-        return;
-      }
       runtime.activeGoal = loaded.goal;
-      if (runtime.activeGoal.safetyResetPending) {
-        runtime.activeGoal = resetGoalSafetyEpoch(runtime.activeGoal);
-      }
-      runtime.recordGoalUsage(runtime.activeGoal, ctx);
-      if (runtime.limitActiveGoalForBudget(ctx, false)) return;
-      if (runtime.enforceAutomaticTurnLimit(ctx, false) || runtime.enforceNoProgressLimit(ctx)) {
-        return;
-      }
-      if (!runtime.goalToolsAvailable()) {
-        runtime.pauseGoalForUnavailableTools(ctx, false);
-        return;
-      }
-      runtime.persistGoal(runtime.activeGoal);
-      if (!runtime.ownsWorkflow(runtime.activeGoal)) return;
-      const restoredGoalId = runtime.activeGoal.id;
-      runtime.ensureGoalContextContract(ctx, runtime.activeGoal);
-      if (runtime.activeGoal?.id !== restoredGoalId || runtime.activeGoal.status !== "active" || !runtime.ownsWorkflow(runtime.activeGoal)) {
+      if (!runtime.acquireWorkflow()) {
+        runtime.deferWorkflowRestore(ctx, loaded.goal.id, () => retryPendingRestore(ctx));
+        runtime.updateStatus(ctx, loaded.goal);
+        notifyTerminal(ctx.ui, "Goal recovery is waiting for the current workflow to finish, then it will continue automatically.", "info");
         return;
       }
-      runtime.updateStatus(ctx, runtime.activeGoal);
-      runtime.restoreGoalWaitTimer(ctx);
+      activateRestoredGoal(ctx, loaded.goal, false);
       return;
     }
     runtime.activeGoal = loaded.goal;
@@ -694,7 +699,7 @@ function registerGoalLifecycle(pi, runtime, runController, options = {}) {
     runController.unbindSession();
     runtime.closeMenuSession();
     runtime.clearGoalWaitTimer();
-    if (runtime.activeGoal) {
+    if (runtime.activeGoal && !runtime.hasPendingWorkflowRestore()) {
       if (runtime.activeGoal.status === "active") {
         runtime.recordGoalUsage(runtime.activeGoal, ctx, false);
       }
@@ -758,6 +763,7 @@ function registerGoalLifecycle(pi, runtime, runController, options = {}) {
     runtime.scheduleContinuationDispatch(ctx, runtime.activeGoal.id);
   });
   pi.on("input", (event, ctx) => {
+    if (runtime.hasPendingWorkflowRestore()) return;
     if (event.source === "extension") {
       if (runtime.consumeCancelledGoalPrompt(event.text) || runtime.consumeCancelledContinuationPrompt(event.text) || runtime.consumeStaleOwnedGoalPrompt(event.text)) {
         return { action: "handled" };
@@ -786,6 +792,7 @@ function registerGoalLifecycle(pi, runtime, runController, options = {}) {
     runtime.resetActiveSafetyEpoch(ctx);
   });
   pi.on("message_start", (event, ctx) => {
+    if (runtime.hasPendingWorkflowRestore()) return;
     const message = event.message;
     if (message.role === "assistant" && runtime.activeGoal?.status === "paused" && runtime.guardAbortGoalId === runtime.activeGoal.id) {
       abortCurrentTurn(ctx);
@@ -831,6 +838,7 @@ function registerGoalLifecycle(pi, runtime, runController, options = {}) {
     runtime.updateStatus(ctx, runtime.activeGoal);
   });
   pi.on("context", (event, ctx) => {
+    if (runtime.hasPendingWorkflowRestore()) return;
     const keptMessages = event.messages.filter(
       (message) => runtime.keepBudgetWrapUpMessage(message)
     );
@@ -844,6 +852,7 @@ function registerGoalLifecycle(pi, runtime, runController, options = {}) {
     }
   });
   pi.on("tool_call", (event, ctx) => {
+    if (runtime.hasPendingWorkflowRestore()) return;
     runtime.markAgentToolAttempted();
     if (runtime.activeGoal?.status === "budget_limited" && runtime.budgetWrapUp?.goalId === runtime.activeGoal.id && event.toolName !== "goal_complete") {
       abortCurrentTurn(ctx);
@@ -864,6 +873,7 @@ function registerGoalLifecycle(pi, runtime, runController, options = {}) {
     };
   });
   pi.on("tool_execution_end", (_event, ctx) => {
+    if (runtime.hasPendingWorkflowRestore()) return;
     if (runtime.activeGoal?.status === "budget_limited" && runtime.budgetWrapUp?.goalId === runtime.activeGoal.id && !runtime.budgetWrapUp.delivered) {
       runtime.queueBudgetWrapUp(ctx, runtime.activeGoal);
       return;
@@ -875,9 +885,10 @@ function registerGoalLifecycle(pi, runtime, runController, options = {}) {
     runtime.persistGoal(runtime.activeGoal);
     runtime.updateStatus(ctx, runtime.activeGoal);
     if (runtime.limitActiveGoalForBudget(ctx, true)) return;
-    if (!runtime.goalToolsAvailable()) runtime.pauseGoalForUnavailableTools(ctx);
+    if (!runtime.ensureGoalToolsActive()) runtime.reportUnavailableGoalTools(ctx);
   });
   pi.on("before_agent_start", (event, ctx) => {
+    if (runtime.hasPendingWorkflowRestore()) return;
     runtime.clearAgentRun();
     if (runtime.guardAbortGoalId) runtime.guardAbortGoalId = void 0;
     const goalPrompt = runtime.consumeOwnedGoalPrompt(event.prompt);
@@ -911,8 +922,8 @@ function registerGoalLifecycle(pi, runtime, runController, options = {}) {
     }
     if (ownedPromptGoalId && ownedPromptGoalId !== runtime.activeGoal?.id) {
       runtime.beginAgentRun(ownedPromptGoalId, runOrigin);
-      if (runtime.activeGoal?.status === "active" && !runtime.goalToolsAvailable()) {
-        runtime.pauseGoalForUnavailableTools(ctx, false);
+      if (runtime.activeGoal?.status === "active" && !runtime.ensureGoalToolsActive()) {
+        runtime.reportUnavailableGoalTools(ctx, false);
       }
       abortCurrentTurn(ctx);
       return;
@@ -921,9 +932,9 @@ function registerGoalLifecycle(pi, runtime, runController, options = {}) {
       return goalContractBoundaryResult(ctx);
     }
     runtime.beginAgentRun(runtime.activeGoal.id, runOrigin);
-    if (!runtime.goalToolsAvailable()) {
-      runtime.pauseGoalForUnavailableTools(ctx, ownedPromptGoalId !== void 0);
-      return goalContractBoundaryResult(ctx);
+    if (!runtime.ensureGoalToolsActive()) {
+      runtime.reportUnavailableGoalTools(ctx, true);
+      return;
     }
     if (goalPrompt?.resetSafetyEpoch && goalPromptGoalId === runtime.activeGoal.id) {
       runtime.activeGoal = resetGoalSafetyEpoch(runtime.activeGoal);
@@ -933,6 +944,7 @@ function registerGoalLifecycle(pi, runtime, runController, options = {}) {
     return goalContractBoundaryResult(ctx, runtime.activeGoal);
   });
   pi.on("agent_start", (_event, _ctx) => {
+    if (runtime.hasPendingWorkflowRestore()) return;
     const activeGoal = runtime.activeGoal;
     if (activeGoal && runtime.guardAbortGoalId === activeGoal.id && activeGoal.status === "paused") {
       if (runtime.consumeQueuedNonGoalFollowUpForAgentStart()) {
@@ -945,12 +957,19 @@ function registerGoalLifecycle(pi, runtime, runController, options = {}) {
     runtime.beginRecoveryRunIfNeeded();
   });
   pi.on("turn_end", (event, ctx) => {
+    if (runtime.hasPendingWorkflowRestore()) return;
     runtime.recordAutomaticTurn(ctx, event.message);
     if (runtime.activeGoal?.status !== "active") {
       runtime.ensureInactiveGoalContextContract(ctx);
     }
   });
   pi.on("agent_end", (event, ctx) => {
+    if (runtime.hasPendingWorkflowRestore()) {
+      if (findFinalAssistantMessage(event.messages)?.stopReason === "aborted") {
+        runtime.cancelPendingWorkflowRestore();
+      }
+      return;
+    }
     const run = runtime.finishAgentRun();
     if (run.goalId === null) return;
     if (!runtime.canRecordGoalUsage() && !runtime.hasActiveBudgetWrapUp()) return;
@@ -973,15 +992,34 @@ function registerGoalLifecycle(pi, runtime, runController, options = {}) {
     runtime.recordGoalUsage(runtime.activeGoal, ctx);
     if (finalAssistant?.stopReason === "aborted") {
       runtime.clearGoalRecoveryForGoal(goalId);
-      stopGoalAfterAgentEnd(ctx, runtime.activeGoal, finalAssistant, "paused");
+      runtime.cancelContinuationWork();
+      const { waiting: _waiting, ...interruptedGoal } = runtime.activeGoal;
+      runtime.activeGoal = { ...interruptedGoal, updatedAt: Date.now() };
+      const abort = goalAbortContext(ctx, runtime.activeGoal, finalAssistant);
+      runtime.persistGoal(runtime.activeGoal);
+      runtime.recordGoalTransition(runtime.activeGoal, {
+        cause: abort.cause,
+        initiator: abort.initiator,
+        previousStatus: "active",
+        status: "active",
+        abortReason: abort.abortReason,
+        invocationId: abort.invocationId,
+        runId: abort.runId
+      });
+      runtime.updateStatus(ctx, runtime.activeGoal);
+      notifyTerminal(
+        ctx.ui,
+        "Goal work was interrupted. The goal remains active and will continue on the next wake or message.",
+        "warning"
+      );
       return;
     }
     if (finalAssistant?.stopReason === "error") {
       if (isRetryableGoalInterruption(finalAssistant)) {
         if (run.origin === "automatic" && runtime.enforceAutomaticTurnLimit(ctx, true)) return;
         if (runtime.limitActiveGoalForBudget(ctx, false)) return;
-        if (!runtime.goalToolsAvailable()) {
-          runtime.pauseGoalForUnavailableTools(ctx);
+        if (!runtime.ensureGoalToolsActive()) {
+          runtime.reportUnavailableGoalTools(ctx);
           return;
         }
         runtime.goalRecovery = {
@@ -1006,8 +1044,8 @@ function registerGoalLifecycle(pi, runtime, runController, options = {}) {
     }
     runtime.clearGoalRecoveryForGoal(goalId);
     if (runtime.limitActiveGoalForBudget(ctx, false)) return;
-    if (!runtime.goalToolsAvailable()) {
-      runtime.pauseGoalForUnavailableTools(ctx);
+    if (!runtime.ensureGoalToolsActive()) {
+      runtime.reportUnavailableGoalTools(ctx);
       return;
     }
     if (run.origin === "automatic" && runtime.recordAutomaticRunProgress(
@@ -1025,6 +1063,10 @@ function registerGoalLifecycle(pi, runtime, runController, options = {}) {
     runtime.requestContinuation(currentGoal);
   });
   pi.on("agent_settled", (_event, ctx) => {
+    if (runtime.hasPendingWorkflowRestore()) {
+      retryPendingRestore(ctx);
+      return;
+    }
     if (runtime.activeGoal?.status === "active" && !runtime.ownsWorkflow(runtime.activeGoal)) {
       runtime.cancelContinuationWork();
       runtime.clearGoalRecovery();
@@ -1048,6 +1090,38 @@ function registerGoalLifecycle(pi, runtime, runController, options = {}) {
     runtime.beginAgentRun(activeGoalId ?? null, activeGoalId ? "manual" : void 0);
     if (resetSafetyEpoch && activeGoalId) runtime.resetActiveSafetyEpoch(ctx);
   }
+  function goalAbortContext(ctx, goal2, assistant) {
+    const entries = ctx.sessionManager?.getBranch?.() ?? ctx.sessionManager?.getEntries?.() ?? [];
+    let latestTransition = -1;
+    for (let index = entries.length - 1; index >= 0; index -= 1) {
+      const entry = entries[index];
+      if (entry?.type !== "custom" || !entry.data || typeof entry.data !== "object") continue;
+      if (entry.customType === "goal-transition" && entry.data.goalId === goal2.id) {
+        latestTransition = index;
+        break;
+      }
+    }
+    for (let index = entries.length - 1; index > latestTransition; index -= 1) {
+      const entry = entries[index];
+      const data = entry?.type === "custom" && entry.customType === "goal-abort-context" && entry.data && typeof entry.data === "object" ? entry.data : void 0;
+      if (!data || data.goalId !== goal2.id) continue;
+      const initiator = ["person", "agent", "engine", "worker", "host", "ui", "failure"].includes(data.initiator) ? data.initiator : "worker";
+      return {
+        cause: typeof data.cause === "string" && data.cause ? data.cause : "worker_abort",
+        initiator,
+        abortReason: typeof data.abortReason === "string" && data.abortReason ? data.abortReason : assistant.errorMessage ?? "Request aborted",
+        invocationId: typeof data.invocationId === "string" && data.invocationId ? data.invocationId : `goal:${goal2.id}:${goal2.iteration}`,
+        runId: typeof data.runId === "string" && data.runId ? data.runId : void 0
+      };
+    }
+    return {
+      cause: "provider_abort",
+      initiator: "failure",
+      abortReason: assistant.errorMessage ?? "Request aborted",
+      invocationId: `goal:${goal2.id}:${goal2.iteration}`,
+      runId: void 0
+    };
+  }
   function stopGoalAfterAgentEnd(ctx, goal2, assistant, status) {
     const stoppedGoal = runtime.stopActiveGoal(ctx, {
       kind: "agent_interruption",
@@ -1300,7 +1374,13 @@ var GoalRunController = class {
       return;
     }
     this.runtime.setTerminalReason(goal2.id, reason ?? "goal cancelled by managed run");
-    this.commands.pauseGoal(session.ctx);
+    this.commands.pauseGoal(session.ctx, {
+      cause: "managed_cancel",
+      initiator: "host",
+      reason: reason ?? "goal cancelled by managed run",
+      invocationId: `managed:${run.runId}`,
+      runId: run.runId
+    });
   }
   handleGoalState(snapshot) {
     const run = this.run;
