From now on Agents will be a first class citizen in Laser, where users will have a page for creating and managing agents, agents include alot of parameters like "system_instructions", model, "support_subagents" (which mean whether this agent can create multiple subagents or not) and alot of others!, laser will not be a single agent anymore


- The right fleet is gonna be for long running commands and subagents all rendered as background tasks, with the agent have the option when running the command to decide it will run in the background by a flag and there will be a hard timout duration that will make a command to be switched to a background task even it was not a background command task by the flag
- when user define an agent he will.. 
  - name/id
  - description
  - supportsSubagents, if enabled..
    - agents it can run as subagent as multi-select (just the agent there is no specific details for the relation here)
  - flag for "Scoped Skills" which means that this agent will have access to these skills only, if not enabled (which is the default)
    - if this flag enabled it should list all the current availble skills from the sources that pi-coding-agent get the skills from at the definition time, if scoped skill for an agent got deleted for any reason, Laser should have a gentle way to flag a yellow warning based on periodic validations to tell the user that there is a harness warning and take the user's hand to the exaact problem in the edit agent page. if this one disabled then the agent will behave normally taking access to all skills in project and global
- Each main agent or a top-level agent should have a separate live visualization mode. So when the user navigates to the live visualization mode, this page will show, using React Flow, a live representation of nodes. Each node contains important high level information for the user about this node (session or subagent). Each node will be an agent working with the tree architecture showing which agent created which sub-agent, and which sub-agent created which sub-agent. And user will be able to view all the live agents, and if he toggled to view the ended sub-agents, whether they failed or done or whatever, he can do that too, in a super visually appealing page that gives him a perfect bird's eye view of what is exactly happening in realtime. And when the user wants to navigate to this exact sub-session, he can do that just by clicking a button in the node to go to the chat, see what's happening exactly, take control to this sub-session, So he can send a message to interrupt or send a message to queue the agent (user can send a message from inside the chat of the session only not from outside), just like exactly he is in the current chat implementation, but he is in the sub-agent chat. And he also can navigate to this exact chat from the sub-session inside the Sessions left sidebar in the screen when he navigated back to the chat interface. The visual flow page for the session should be read-only, just for the user to read high-level information about each session/node in the multi-agent architecture. But if he wants to take control or to send a message in any of these agents, he navigates to a normal chat interface based on his clicking. And the events that is happening between agents and each other should be reflected as a gentle info bubble inside the node that own this event, an event inside, like received a message or sent a message to other agent. Just the gentle info bubble inside this node will appear for a period of time and then disappear, just to give live and real-time feel of what's happening.
- read the agent-harness-architecture file to understand how agents will start sub agents
- built-in non-deletable and non-editable agents: for now they will just be the namer, chat and the beam agents, they should be visible in the Agents page but not primarly in the screen (maybe at the bottom). each is gonna be integrated inside the Laser Coding Agent in its own way
- The Beam agent: its the Laser's stunning, fast AI helper that uses the same internal implementation of agents implemented inside Laser (with respect to the pi-laser contract of responsibilites)
- The Beam agent should run in an average (smart enough) but fast model, think of gpt-5.6-luna by today's standards (and run search to understand the standard more), the engine should open a popup with a default of the average model whenever the user connected his first provider in order to select his beam model (with clear information of what will be beam doing)
- Beam has exactly one interaction point: a special green spark icon at the bottom left of Laser, beside the Settings icon. It is always available in chat, Settings, and logs. Clicking it opens a bubble that grows out from the icon with a perfect animation and contains the normal chat interface. Before the first message, the middle of the chat gently hints that Beam is the user's AI assistant for Laser. Sending the first message creates a new Beam chat in Beam's sessions. There are no other Beam entry points elsewhere in Laser.
- Beam is provided with a special skill available only to Beam in order to have agentic access across everything related to Laser: sessions, logs, agents, settings, and how to navigate the file system to answer any Laser-related question. It needs no Beam-specific tools because Laser saves its state to the file system in real time; provide it with the normal read, write, edit, bash, and web-access tools.
- The Beam agent will have its own sessions (with a small style not obvious tweaks to the UI, just to say this is a special session), its sessions are gonna be listing noramlly like it is a project in the left sidebar but with diff icon than the folder icon because it is not an project it its a built in feature inside Laser
- The Chat agent: it is a special agent for the chat tab, chat tab gonna be the first tab in the left side bar (imagine "Chat | Code" tabs on the top of the sessions search field) but it is gonna be simpler when level sessions for just chats that are not related to any project with a prompt
- The Namer agent: the best model for this agent is one that has the perfect balance between being fast and cheap (when the user connect the first provider it should run a benchmark to accross programatially nominated cheap models (not to include expensive model for it in this benchmark) to select the perfect model for this simple agent that will run alot for a small simple task), its task gonna be simply to naming things based on a small context about what is happening in the session like...
  - in-progress tool call just after the tool call invocation (don't wait until it ends)
  - in-progress aggregated-actions in the chat view
  - a session that just started, to give it a name based on the prompt that don't exceed 25-30 chars
- subagents always in sub-sessions that appear in the left sidebar as a normal chat that the user can fully control


notes: 
- The current UI/UX for what is being rendered inside the panel sucks because it does not respect its size! it should be smart enough to decide what will be the perfect view for the user in this current panel size, with totally different UI if it is taking the full sidebar panel, with totally different UI view if it is expanded to full screen! keep this in mind while you are implementing the visual node-based multi-agent arch live view

- The current defaults of the main agent of Pi should be included as an editable agent named "default" with the current system prompt and it should be the default unless another agent gets selected as the default (default only means it will be the default when opening a new session (this should be included in a "i" mark beside the default toggle in the agents)). The current default agent cannot be deleted; after another agent is selected as the default, the former default agent can be deleted.
- agents should integrate perfectly with current architecture and modules like the /goal command so the context of the goal will be shared to all subagents with the agent know exactly his role towards this goal
- worktrees should be not optional for subagents, you are a subagent then you work on separate worktree!
- all subagents should be non blocking! all run the background, it should not be a choice from the parent agent!
- you will find the react-flow skill and the pi-subagents reference (the one we are implementing sub-agents on top of right now and we are planning to replace with our own implementation, while copy from it what aligns with our new vision and the things that they put alot of clear investement in so we don't re invent the wheel)
