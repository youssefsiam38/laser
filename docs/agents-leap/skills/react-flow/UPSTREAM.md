# Source provenance

- Repository: https://github.com/framara/react-flow-skill
- Commit: `a224eeb6844200dc743fcd35a533744c02ab58f7`
- Skill: `SKILL.md` at repository root; installed with its references and license.
- Requested for Laser's agents LEAP by the user.
- Installed with the Codex skill-installer helper into this project, not the global skill directory. The helper does not manage this repository's separate `npx skills` lockfile.

The upstream skill and references are preserved. This provenance file is local. Follow Laser's `AGENTS.md`, assistant-ui inventory, theme and responsibility contracts when applying the examples. In particular, use mapped tokens rather than copying literal visual values, and keep graph presentation separate from execution state.

The canonical skill files are grouped with the LEAP brief under `docs/agents-leap/skills/react-flow`. `.agents/skills/react-flow` is a relative symlink to this directory so the existing skill path continues to resolve.
