# The element inventory — every assistant-ui element, claimed

Status: **binding.** Every element in the assistant-ui catalog is listed here
with the laser surface that owns it, or an explicit reason it does not
apply. Nothing is left unclaimed by accident.

## The rule

Application chrome not represented by a catalog element stays in the shell:
`LinkDestination` is one pointer-transparent bottom-left URL status strip, also
covering keyboard focus and portalled links. It never fetches a preview. Provider
connection waits reuse `GenerationLoader` beside their active controls; saving
credentials must not be mislabeled as an API test.

**Do not hand-roll a component that exists in the catalog.** Install it
(`npx assistant-ui@latest add <name>`), then style it to
[`packages/ui/DESIGN.md`](../packages/ui/DESIGN.md). Editing the copied
source is expected and is the point of a registry; rebuilding it from scratch
is not.

Two element families behave differently:

- **AUI-connected** (`*.aui.tsx`) read the runtime directly. They drop into
  our thread because we already run an assistant-ui runtime.
- **Standalone** are props-driven specimens with demo props (`visibleCount`,
  `cycle`, fixed `min-h`). De-demo them: strip the demo props, feed them from
  a real payload, and delete any hardcoded size.

When an element is close but not right, the answer is to copy it and change
it, not to start from an empty file. Record the divergence in the row.

## Reasoning

| Element | laser surface |
| --- | --- |
| Loader | Every loading state: startup restoration, session hydration, settings, features, models and logs — **adopted**: `packages/ui/src/components/assistant-ui/elements/loading-state.tsx` (`GenerationLoader`, `StartupRestorationGate`). The startup form adapts Magic UI's Animated Beam pattern into rounded, mark-shaped rails around the transparent Laser mark and the real remembered-session hydration boundary; it never mounts the false empty shell underneath. Both forms use theme and motion tokens and hold still under reduced motion. Wired to startup, session hydration (`thread/Thread.tsx`), model pickers, Settings, Features, Providers and models, Logs, and a log payload before its first bytes. Button-local busy icons use `--animate-busy`, the app's own keyframes, never Tailwind's defaults (`test/design-system.test.ts`). **`components/ui/skeleton.tsx` stays** for waits whose content shape is already known |
| Thinking indicator | The assistant's thinking block while a turn streams — **adopted**: `elements/thinking-indicator.tsx`, the header of the reasoning block while it streams (inside `reasoning`'s trigger). The dot is the app's `working` `StatusDot`, the elapsed value is `typed` |
| Streaming text | Assistant text parts during stream; pairs with the streaming caret in DESIGN.md — **adopted**: `elements/streaming-text.tsx`. `StreamingText` wraps the markdown part and puts the caret on its last block while the part runs; `StreamingCaret` alone is what a turn shows before its first token. The registry's word-by-word blue landing is not emulated: the part is markdown rendered by `MarkdownTextPrimitive`, and re-parsing it per word would be theatre (R3). The hand-rolled caret class in `MarkdownText` is gone |
| Typing indicator | Work that is going but sending no deltas — the honest substitute for a typewriter — **adopted**: `elements/typing-indicator.tsx`. Dots step through the attention pulse with a `--motion-slow` stagger, at `--ink-2` and 8px — `--ink-3` at 6px was a flicker nobody could see on `--surface`. `showLabel` defaults to **on**, so the state is always in words: under reduced motion the dots do not move at all, and a state with no words is no state. **Not mounted since M13-T26**: the fleet row says its state in words rather than in dots, and the thread never showed it because the main session streams. Claimed for the next surface that watches something silent |
| Reasoning effort | The composer's thinking-level control, showing the levels *this session's model accepts* — **adopted**: `packages/ui/src/components/assistant-ui/elements/reasoning-effort.tsx`; `ReasoningEffort` is a real `radiogroup` (one tab stop, arrows, Home, End) and `ThinkingEffort` binds it to the session: inline at `lg`, in a popover below. The demo's "budget spent" bar is gone — Pi reports no thinking budget (R3). The seven levels are filtered by `ModelCatalogEntry.thinkingLevels` (`useSupportedThinkingLevels`, one cached `pi/models/catalog` per directory): a control appears only if it works here, so a non-reasoning model shows no segments at all — just the reason, in a tooltip where the control would have been (R2). The regenerate menu is filtered by the same hook. Replaces `thread/ThinkingSlider.tsx` (deleted) |
| Guardrail notice | Project trust declined, untrusted project, insecure origin on mobile, `disableBuiltins` breaking an agent — **adopted**: `elements/guardrail-notice.tsx`, `attention` tone, alternatives as `Button`s (hidden when there is nothing to offer, R2). Wired: a declined project shows it above the composer (`thread/Thread.tsx` `TrustGuardrail`). **The insecure-origin notice keeps its own body** (`components/mobile/Notices.tsx` `InsecureOriginNotice`), and the reason is written there too: `GuardrailNotice` is a static three-part card — title, explanation, alternatives — and that surface needs three things it has no shape for. It is a row in the phone's `MobileStack` rhythm, not a card; the explanation is behind a "Why?" disclosure because the strip sits above the composer and must stay one line at rest; and what to do about it is an *ordered* list of steps plus struck-through capability chips, neither of which is an alternative to pick. It is dismissible per origin, which the element has no notion of. A `disableBuiltins` warning is still a request to the settings lane |

## Messages

| Element | laser surface |
| --- | --- |
| Message pair | Transcript user/assistant pairing — **adopted**: `elements/message-pair.tsx` as composable pieces (`UserBubble`, `AssistantBody`, `MessageFooter`, `hoverReveal`) because `ThreadPrimitive.Messages` renders one message at a time; `thread/messages.tsx` composes them. The compact bubble uses the shared 14px body scale; controls and metadata keep their existing size. The demo typewriter is gone |
| Message branches | Pi's session tree: the versions of one prompt (M1-T9, M13-T35) — **adopted**: `elements/message-branches.tsx`, under a prompt whose entry has siblings (`thread/entries.ts` `versionsOf`); stepping moves the session onto that version's own last entry (`leafOf`, then `pi/session/navigate`), because navigating onto a prompt would leave the session *before* it. `pi/session/entries` now answers with `leafId`, so the live version is placed and the picker reads "2 / 2" with an accessible "Version 2 of 2"; `index` stays optional for an entry the session no longer holds, where it says "2 versions" rather than guessing (R3). Editing in place and trying a reply again are what put versions here, and this is the promise that neither loses the previous answer |
| Message actions | Copy, edit, try again, and the session-tree menu — **adopted**: `elements/message-actions.tsx`. On a prompt: copy, Edit, then the menu (fork here, jump here, copy session path, view API request); on a reply: copy, Try again, the model-and-thinking menu, then the menu, which also carries "Try again in a new session". Editing and trying again change the **open session**; the fork is the second, clearly named choice one row down, never the primary button (M13-T35). The thumbs are gone (Pi has no feedback channel). A prompt is matched to its entry by ordinal (`entries.ts`); an unpersisted one shows no tree actions (R2). While a reply streams, Edit, Fork and Jump stay live (M13-T46): the engine will not move the leaf mid-turn, so each asks the worker to stop the reply first and then move — `stopFirst` on `pi/session/navigate` / `pi/session/fork`, one request, the worker's own sequence, so a move that fails leaves the session stopped and where it was. The stop is the Stop button's stop, recorded as `aborted` on the branch being left and reachable from the version picker; the tooltip and the menu rows say "stops the reply" beside the action, and no dialog asks again. Only "Try again" waits for the turn |
| Error state | `extension_error`, a failed turn, a crashed worker — **adopted**: `elements/error-state.tsx`. Wired: an error notice renders "`<extension>` failed" with the message; provider attempts under automatic recovery stay silent (D-180), and only the exhausted/non-retryable outcome reaches Stopped run. The crashed-worker card above the composer is a shell request |
| Message queue | **The steer tray**: the waiting messages above the composer (M1-T5, M13-T28, D-149) — **adopted**: `packages/ui/src/components/assistant-ui/elements/message-queue.tsx`; `ComposerQueue` is mounted by `thread/Composer.tsx` directly above the input, reads `s.composer.queue` and draws one **row** per message, not a chip. Enter puts a message in the tray; Cmd/Ctrl+Enter, or the row's own Steer, sends it now. **closed**: per-item remove, edit and steer now exist. The engine still has no per-item verb, so the waiting lane is Laser's own list in the worker (`@lasercode/protocol` `pending.ts`, `worker/src/pending.ts`) and the row's Steer / delete / ⋯ go through the adapter's `ExternalThreadQueueAdapter.move` / `.remove`. A row the engine already holds ("Up next", solid `--live`) still offers none of them, which is R2 unchanged. No "running" row (the status line says that). Replaces `thread/QueueChips.tsx` (deleted) |
| Edit a sent message | A real edit of the open session — **adopted**: `elements/edit-message.tsx`, the editing card only (the at-rest bubble is `message-pair`'s). Titled "Edit message"; Send moves the session back to just before this prompt (`pi/session/navigate`, which is Pi's `navigateTree` — the file is an append-only tree, and the opening message needs no parent because the engine resets the leaf) and sends the new text there as another version, with "In a new session" beside Cancel for the fork. The copy says what changes — "Replaces this message here", plus how many later messages stay on the old version — and while a reply is streaming the card still sends: the copy becomes "Stops the reply being written, then replaces this message here", the worker stops the reply and moves in one request (`stopFirst`), and the stopped reply is recorded on the old version rather than silently aborted (M13-T35, M13-T46, `thread/messages.tsx`). `busy` on the card is only its own send in flight, so one click is one send |
| Feedback dialog | **Not applicable; the copied file is deleted.** The element is a fixed list of canned reasons as chips plus a note. A question's rejection is `{ label, field }` (`packages/ui/src/dialogs/model.ts`) — one declared field, never a list of reasons, so the chips have no source; inventing a set would be putting words in the extension's mouth. The same is true of a tool denial, whose reason is free text. The rejection field is drawn by `elicitation-form` from `DialogBody`, and the tool case by `tool-fallback.aui`'s deny-with-reason |
| Stopped run | An aborted turn or a provider request that could not recover — **adopted**: `elements/stopped-run.tsx`; always says why (R3) and offers Continue only on the last reply while idle (R2). Wired from `message_end.stopReason` through `projection.ts` and `thread/messages.tsx`. Provider failures use the attention tone, a human explanation and one corrective action; recoverable attempts are removed live and on hydration, so a successful retry leaves no transcript debris (D-180). `RunBody` renders agent-run terminal reasons separately |
| Timestamps (day separator) | **Timestamp adopted; day separator deliberately removed (D-105)**: `elements/message-timestamp.tsx` gives every user, assistant and notice message a compact local clock, with a natural full date such as “Today at 4:05 PM” for hover and assistive technology. The separate Today/Yesterday hairline duplicated that information and consumed transcript height, so it no longer renders |
| Speaker identity | Which agent produced a message inside a child session — **adopted**: `elements/speaker-identity.tsx`, rendered as a header when the projection stamps `metadata.custom.laser.speaker` (`{ kind, name, detail }`). **Reachable since D-140**: a child session carries `SessionState.agent` (`agentName`, `subagentName`, `kind`, M13-T1), so the projection fills the speaker for a child's assistant messages (M13-T6) — the field that was missing now exists, and the call site is unchanged |
| Regenerate with | Re-run with a different model or thinking level, in this session — **adopted**: `elements/regenerate-menu.tsx`, a real `DropdownMenu` with a thinking group and a model group (loaded on open through `pi/model/list`). Picking moves the session back to the prompt that produced the reply, takes the engine's own `editorText` for it rather than re-deriving it from the transcript, sets the model or level, and sends it again — so the new answer is a sibling of the old one and the version picker reaches both (M13-T35, `thread/messages.tsx` `AssistantFooter`). "Try again in a new session" in the overflow menu is the same re-run into a fork |
| Confidence marker | **Adopted for instruction provenance, never confidence (D-135)**: `elements/confidence-marker.tsx` retains inline source markers and hover/focus disclosure, with touch-accessible source chips. The request inspector feeds verified captured source ranges; file, skill, engine, environment and extension contributions use semantic tokens. Plain view marks exact boundaries; Markdown keeps the complete chat renderer and exposes the same recorded source inventory. No confidence scores are invented |

## Tool use

| Element | laser surface |
| --- | --- |
| Tool call | The tool row in the transcript — **adopted**: `packages/ui/src/components/assistant-ui/elements/tool-call.tsx`, de-demoed into the row grammar `[icon] verb summary ····· duration ›` and composed from the `tool-fallback` parts; `thread/ToolRow.tsx` is now only the Pi glue (verb/summary from `tool-summary.ts`, body choice, and registering the row so a question can be asked inside it), plus the harness's own `start_agent` row (`StartAgentRow`: who was started, where it is working, Open chat). The demo's `label/activeLabel/query/request/result` strings and `max-w-sm` are gone. The per-element sweep is [`ux-elements-audit.md`](ux-elements-audit.md) |
| Tool timeline | The sequence of tool calls a session made — **adopted**: `packages/ui/src/components/assistant-ui/elements/tool-timeline.tsx`, fed by `toolTimelineFromParts` (verbs and targets from `tool-summary.ts`, per-file churn from `diff.ts`) and `useThreadToolTimeline`; `visibleSteps` and the per-step icon prop are gone. **Wired**: the monitor's "Tools" section (`components/shell/TelemetryPanel.tsx`), open state on the shell context beside `historyOpen`. The monitor rather than a fleet row: the timeline reads the *thread's* parts, which is the session's own tool calls, not a child's |
| Terminal block | `bash` output — **adopted**: `packages/ui/src/components/assistant-ui/elements/terminal-block.tsx`, dark ground in both themes, real exit code and failure state, head/tail elision with "Show all"; the demo's `lines/visibleCount`, `paper`/`ink` variants, `max-w-md` and hard-coded `exit 0` are gone. Replaces `thread/TerminalBlock.tsx` (deleted). **Not used for the logs pane**: its grammar is a command and its exit code, and a log payload is a record, not something anyone ran — `command={entry.kind}` would be a caption that is simply false. What the logs pane actually needed was the *decoder*: captured-output rows (`worker_stderr` and friends) now render on the terminal ground through the shared `elements/ansi-text.tsx`, so escapes read as colour instead of `ESC[31m` noise. That file also de-duplicates the copy of `AnsiText` that `StreamBody` was carrying Also the fleet's task detail (`components/fleet/FleetPanel.tsx` `TaskDetail`, M13-T60): the same element with three opt-ins the transcript never passes — `follow` keeps a live command's end in view without yanking back a reader who scrolled up and never moves a finished one, `truncatedHead` is the one-line "Showing the end of the output." for the tail the host serves from byte N in place of the block's own elision, and `ansi` decodes colour through `elements/ansi-text.tsx`; the detail's Command and Exit-code fields went, since the block's header says both |
| Code diff | `edit` and `write` results — **adopted**: `packages/ui/src/components/assistant-ui/elements/code-diff.tsx`, rendering a `DiffView` from `diff.ts` with two line-number gutters, hunk headers, a cap and a scroll region; exports `CodeDiffRows`/`DiffStat`; `DiffStat` is reused by `file-tree` and `tool-timeline`. `preview/DiffPreview.tsx`, which drew a whole patch through `CodeDiffRows`, is not mounted; file removed (M13-T61) — it had no caller since the document panels went (D-147), and a diff in the transcript is `DiffBody` in `thread/ToolRow.tsx`. If a file viewer returns, it draws through `CodeDiffRows` again rather than reviving that wrapper. Source tokens are layered lazily inside each preserved diff line using the same path-inferred Shiki grammar and theme as transcript fences; `read` uses the standalone source form through `tool-code-highlights.tsx`. The demo's `cycle` counter and per-line animation delay are gone. Replaces `thread/DiffBlock.tsx` (deleted) |
| Reviewable diff | **Not applicable yet; the copied file is deleted.** Reviewing and applying a subset of hunks needs a `pi/project/apply` method the protocol does not have, so the element could only ever have been mounted with its Apply inert — a control that does nothing (R2). The Create PR dialog in `thread/ProjectLine.tsx` shows commands today. Re-install it in the same change that adds the protocol method, not before: an unmountable file in the tree reads as done work and is not |
| File tree | Files a session touched — **adopted**: `packages/ui/src/components/assistant-ui/elements/file-tree.tsx`, `fileTreeFromChanges` builds the nodes (single-child folder chains collapse; tested in `test/elements/file-tree.test.ts`) and `useSessionFileChanges` collects per-file churn from the thread's edit/write calls; `visibleCount` is gone. **Wired**: the telemetry rail's "Files changed" section (`components/shell/TelemetryPanel.tsx`) |
| Elicitation form | An extension question with fields to fill — **adopted**: `packages/ui/src/components/assistant-ui/elements/elicitation-form.tsx`. `DialogBody` (`packages/ui/src/dialogs/DialogBody.tsx`) is the controller (state, keyboard, timeout, submit); this draws any question with fields, including the rejection reason after "No". The demo's static field values became real controls that follow `DialogField`. Where it lands (D-147, `ux-fleet.md` "Questions"): inside the tool row that raised it (`dialogs/InlineDialogs.tsx` `ToolRowDialog`, and `thread/ToolRow.tsx` `InterruptFooter` for a question that arrived on the tool call itself), otherwise a card above the composer (`ThreadDialogCards`, mounted by `thread/Thread.tsx`); `WaitingNotice` is the composer's one line when the row is off screen. Never a column, never a sheet |
| Server panel | MCP servers a project configures, in Settings → Features (there is no Packages tab: people enable features, never install packages, invariant 6b). **Not installed**, for the same reason as the MCP config dialog row: the protocol carries no MCP server list yet, so the element would render invented rows. Protocol first — `pi/mcp/*` on `@lasercode/protocol`, then this element and the config dialog together |
| Tool failure | A tool row with `isError` — **adopted**: `packages/ui/src/components/assistant-ui/elements/tool-error.tsx`, the danger hairline on the row plus the error text in danger ink; `compact` is the three-line excerpt a collapsed row shows. The demo's attempt counter and Retry/Skip buttons are gone: Pi has no per-tool retry, and a dead control is not shown (R2) |
| Permission grant | Tool approval with scope options — **adopted**: `elements/permission-grant.tsx`, chosen by `DecisionBody` for a one-question `choice` with a mode-changing option ("Always allow"); "this grants" names each scope-widening option before it is pressed. Scopes are the payload's options, not a fixed Deny / Session / Always. **Enter never grants**: the registry autofocuses the first option, which here is the widest one, so `data-autofocus` moved to the decline/cancel control and the arrows walk from there into the options (`DecisionBody`'s roving handler). The same rule is applied in Approval card and Tool fallback — one rule, every surface that gates execution. **Pi's project-trust prompt keeps its own body** (`components/shell/TrustDialog.tsx`, where the reason is also written), for two things the element cannot express and both matter more than a shared drawing: its `message` is a single string, while that question is unanswerable without the directory it concerns and the chips naming which files in it are trust-gated |
| Computer use | **Not applicable.** laser does not drive a computer |
| Code runner | **Not applicable in v1.** No sandboxed execution surface; revisit if Pi gains one |

## Knowledge

| Element | laser surface |
| --- | --- |
| Web search | **Not applicable; the copied file and the web-access panel adapter are deleted (D-61, confirmed by M13-T26).** The package's tool call already renders as a collapsible transcript row with its request and results, which is where the reader is. A second list of the same results was the panel system's clearest example of showing one thing twice |
| Inline citation | Citations inside assistant text from web-access — **adopted as the chip style only**: `packages/ui/src/components/assistant-ui/elements/inline-citation.tsx` exports `citationChip`, which `markdown-text` draws GFM footnote references (`[^1]`) with — the form citations actually take in a transcript today. The hover-card `Citation`/`InlineCitation` pair was **deleted** rather than shipped unmounted: nothing in the protocol emits a numbered source list for it to preview, and the same standard deleted `document-reference`, `memory-chips`, `reviewable-diff` and `research-report`. It comes back with the producer (see Sources) |
| Image generation | **Not applicable in v1.** No image model surface |
| Retrieval chunks | **Not applicable in v1.** No RAG in Pi |
| Document reference | **Still not installed.** `pi/project/files` exists, so the data objection is gone and `@file` mentions work — but they render as popover rows, not as this card. Install it when a surface appears that shows a file reference as a card of its own; nothing does today |
| Memory | **Not applicable yet; the copied file is deleted.** Chips need the list of context files a turn loaded (`AGENTS.md`, `.pi/` context, skills, memory). Nothing reports it: there is no per-turn context-file list on `SessionState` or any session update, so the element could only be fed with a guess. Protocol first (a `{ path, kind }` list on the turn), then re-install |
| Research report | **Not applicable; the copied file is deleted.** It rendered a plan — a noun nothing persists since D-140 — and even while plans existed it was a second drawing of the same payload, with no rule saying which appeared. One thing gets one presentation |
| Map | **Not applicable.** No geographic data |

## Structured output

| Element | laser surface |
| --- | --- |
| Data table | The provider-grouped model catalog — **adopted**: `packages/ui/src/components/assistant-ui/elements/data-table.tsx`, column-driven (`columns` + `rows` + `rowKey`, a real `<table>` with a caption, scrolling inside its own container). Each row opens with the settings-panel element's **switch** (`SettingsSwitch`, the person's on/off; the off side is Laser's own `disabledModels` list, since the engine's allow-list has no negation) and carries its offer state (`settings/model.ts`, `modelOfferState`): nothing when the pickers show it, an outline **Switched off** pill for the person's own choice, an outline **Provider not signed in** pill when the key is missing, an `--attention` **Hidden by Enabled models** pill when only the allow-list hides it — the switch lets it back in; the row is never dimmed below the contrast floor to show that state. Each provider header carries **Enable all** / **Disable all**, and the section is viewed through a `DropdownMenu` radio group (All · Enabled · Hidden) beside the search; the pattern editor is folded under *Advanced* (M13-T49, D-161). Mounted by `settings/ModelsTab.tsx` and, for the models Namer tried, `agents/page/BuiltinPanel.tsx`. **Not the Features cards**: those are two-state capability controls, not records. **Not the logs list**: five thousand rows need the windowed list in `logs/LogsScreen.tsx` |
| Number ticker | Live token, time and cost counters — **adopted**: `elements/number-ticker.tsx`, inline and `typed`: `value` is an already formatted string whose digits roll on `--motion-slow`. Wired on the status line's turn time and token count (`thread/StatusLine.tsx`), the monitor's total tokens (`shell/TelemetryPanel.tsx`) and the agents map's node clock (`agents/map/NodeParts.tsx`). Only a number that changes while you watch it rolls; a settled value is a plain span |
| Chart | Spend over turns in the telemetry rail — **adopted**: `packages/ui/src/components/assistant-ui/elements/chart.tsx`, fed by `spendSeries()` (cumulative cost per assistant turn from the session file) in `shell/TelemetryPanel.tsx`; drawn only once there are two turns. `visibleCount` and the rising/falling colouring are gone: a series is drawn whole and a rising spend is not good news |
| Web preview | **Deferred.** Embeds are out of scope for v1 (D-18); the element is claimed for when that changes |
| Diagram | Mermaid fences in the transcript — see Mermaid diagram under Renderers; one element covers both rows |
| Flow graph | **Not applicable since M13-T26; the copied file is deleted.** It drew a `plan` panel's declared graph, and plans went with the panels. **Not the agents live map (D-140)**: that is Laser-owned on `@xyflow/react` — pure model and run tree in `packages/ui/src/agents/` (`model.ts`, `run-tree.ts`), page and map components under `packages/ui/src/components/agents/` (M13-T7) — because it is a per-session surface with its own layouts by measured size, ancestry edges, status and transient event bubbles ([`agents.md`](agents.md) §5) |
| Activity graph | Per-project session activity; pairs with Heat graph. **Not installed in wave 3**: no surface renders per-project activity (the sessions panel groups by project but shows rows, not history; the fleet is work, not sessions). Claimed for a project header or Settings → Projects, with the `heat-graph` dependency added then; installing it now would leave a dead file |
| Math | Math in markdown — **rendered through `markdown-text`**: remark-math + rehype-katex with the package's delimiter normalizer and currency guard (`$5` stays a price). `elements-math-block` itself is **not installed**: it is a card of hand-authored steps (`Frac`, `Sup`, `Sub`) that Pi never emits; installing it would leave a dead file |
| Spec sheet | Labelled metadata rows: model, thinking, cwd, ids — **adopted**: `packages/ui/src/components/assistant-ui/elements/spec-sheet.tsx`, typed values are mono and tabular, an empty row is not drawn (R3); `visibleCount` is gone. **Wired**: the captured-request inspector's metadata (`logs/ApiRequestDialog.tsx`, D-97). Its former mount, the run island's body, went with the panels (D-147); `runSpecRows`/`sessionSpecRows` remain in the file with no caller today, for a surface that shows one run's facts with no neighbours already saying them. The fleet's detail rows (`fleet/FleetPanel.tsx` `Field`) are that shape drawn by hand and are the next candidate |
| Comparison | **Not applicable; the copied file is deleted.** The element weighs 2–3 shortlisted options side by side, and laser has no shortlist: `pi/model/list` returns Pi's whole catalogue (a thousand rows in the sandbox), and picking three to feature would be a recommendation Pi never made (R3). The information it carried was real, though, and was missing — so it went where the catalogue already lives: `settings/ModelsTab.tsx`'s `data-table` gained a **Cost / M** column (input and output per million, an em dash where the catalogue does not price a model, never a zero). Vision and reasoning were already badges there |
| Timeline | A run's moments on a time axis — **adopted**: `elements/timeline.tsx`. Two mounts: the agents map's inspector (`agents/map/Inspector.tsx` `timelineOf`: started · what it is doing now, `attention`-toned when the child is paused on a question (`needs_input`, D-158) · how it ended, with the reason), and a goal's durable record (`thread/GoalRecord.tsx`, the goal's moments). Checkpoint history is the Checkpoints element below, because Pi's tree is not a time axis |
| Job progress | Progress, where a producer genuinely has some. **Not mounted since M13-T26; the file stays.** Neither of the fleet's two producers has a percentage or a phase (`ux-fleet.md` R6), and inventing one would be the exact lie this element was de-demoed to avoid. No ETA, no weighted stages, no cancel; it draws nothing when there is nothing to draw, which is currently always |
| Score breakdown | **Retired concept (D-140).** It claimed pi-subagents acceptance-gate results, which the protocol never carried; Laser's harness has no gate verdicts — a run ends `completed` or `blocked` through `complete_agent_run`, and that is a state, not a score. Not installed; nothing to feed it. Adding scored verdicts would be a protocol decision first |

## Agents

| Element | laser surface |
| --- | --- |
| Agent plan | **Not applicable since M13-T26; the copied file is deleted.** It rendered a `plan` panel: phases, steps, done/total. Nothing persists a plan any more — the harness has runs and nothing above them (D-140) — so the element had no producer left, and an element with no producer is a file that rots. If a plan noun returns, this row is where it starts again. History: **was** Phases, steps, done/total — **adopted**: `elements/agent-plan.tsx` replaces `panels/islands/bodies/PlanBody.tsx` (deleted). D-20 by size: collapsible phases with `done/total` and step squares in the dock; columns when maximized, or the declared graph (Flow graph) when one exists; checklist (Todo list) when steps have no phases, runs or edges. Inferred edges are dashed chips labelled `inferred` (R3); usage at the top (R8) |
| Subagent list | **The fleet.** The open session's tree of work (M13-T51) — **adopted**: `elements/subagent-list.tsx` is the list (one session's chronological recursive subtrees, the chat being read marked, a deleted root said so) and `SubagentStrays` the line at the bottom for work whose session was deleted; `components/fleet/FleetPanel.tsx` is the column and the data (the one importer), and `FleetSheet.tsx` wraps that same panel in a sheet below 1280px (D-147, D-160). It separates work into a prominent **In progress** section and a collapsible **Finished** one; done, failed and cancelled keep their distinct row status, finished work is dimmed and never green (D-154), and `needs_input` is a live, attention-toned status that never folds into Finished (D-158). Lifecycle sections project each item's own state immediately: live work belongs in In progress, terminal work in Finished. Required agent ancestors repeat as labelled, uncounted context; section counts and attention include only the actual work shown there. Parentage remains structural in the DOM with a continuous lineage rail, and recursive Clear preserves live/later work and its ancestry without reordering canonical items (D-205). The demo's fake progress bars are gone: no producer has a percentage. **Two producers, both typed** (M13-T26): harness runs (`AgentRun` from `agents/runs/list` and `agents/run`, children under the session that started them) and background commands (`BackgroundTask` from `tasks/list` and `tasks/update`, hanging off the session that ran them). Presentation only — every value arrives on a `FleetItem` and the two actions are callbacks, so the column and the sheet share one list |
| Agent status | **Not mounted; file removed (M13-T61).** `elements/agent-status.tsx` (unmounted since D-147) was the run island's header pill (dot · title · live values, `state` in the five-word vocabulary drawn by `StatusDot`, the decorative pause glyph gone), and islands went with the panels. Nothing imported it. Every surface that shows a run's state today draws `StatusDot` and words directly — the fleet row (`subagent-list`), the agents map node (`agents/map/NodeParts.tsx`), the sessions row (`shell/SessionActivity.tsx`) and the status line — because none of them is a pill with a value budget. The need is claimed for the next surface that is a pill with a value budget: re-install `elements-agent-status` then and restyle it to `StatusDot`'s five words |
| Approval card | A one-question approval, rendered in its tool row or above the composer — **adopted**: `elements/approval-card.tsx`, chosen by `DialogBody` (`packages/ui/src/dialogs/DialogBody.tsx`) for a one-question `confirm` or a plain `choice`; it lands where the Elicitation form does (`dialogs/InlineDialogs.tsx`, `thread/ToolRow.tsx`), and a tool *approval* — assistant-ui's own gate on the tool call — is `tool-fallback.aui`'s `ToolFallbackApproval` in the same footer. Buttons are the payload's options or Yes and the rejection label (R12a); "No" opens the rejection field (never a dead end); no faked "running" state after the answer. **Enter never approves**: focus starts on the decline/cancel control, not on the first choice and not on "Yes" (see Permission grant). `DecisionHeader` here is the header the three decision elements share |
| Recommendation card | **Not applicable.** laser does not recommend actions |
| Artifact card | A file a piece of work produced, shown as a card that opens into the reading view — **adopted**: `elements/artifact-card.tsx`. Its two former mounts went with the panels (M13-T26); it is claimed for the next surface that lists a produced file, and the row exists so that surface copies this rather than starting empty. A `<button>`, not a div with a cursor |
| Todo list | **Not mounted; file removed (M13-T61).** `elements/todo-list.tsx` (unmounted since M13-T26) was chosen by `AgentPlan` for a plan whose steps had no phases, and `agent-plan` is deleted (row above), so nothing imported it: no producer emits a checklist (D-140 has runs and nothing above them). If a plan noun returns, re-install `elements-todo-list` for the phase-less case and give it the six-state vocabulary the deleted copy had (pending · running · done · failed · skipped · blocked), drawn by `StatusDot` |
| Agent card | An agent definition on the **Agents page** (D-140, M13-T5, M13-T67): one card per `AgentDefinition` from `agents/list` — name, description, model, whether it may start agents and which, scoped-skill state, and any `AgentWarning` as a gentle yellow mark that deep-links to the field; `default` carries the default toggle with its `i` explainer; Beam, Chat and Namer sit at the bottom with editable system instructions and model but no delete or identity edit. The instruction textarea is paired with a searchable **Insert field** popover (D-175): human labels and descriptions only, inserting the selected live value at the caret without requiring template syntax knowledge. Since M13-T101, the shared editor offers **Edit** and **Highlighted source** modes: Shiki colours the exact Markdown source; recognized template fields open keyboard- and touch-accessible disclosures showing a current editor value with provenance or why the value resolves only during a run. Literal and malformed template text stays source, and the default instructions reuse the same read-only source surface. **Adopted**: `elements/agent-card.tsx` (installed from the registry, restyled to tokens; version/provider/endpoint/Connect dropped, `facts`/`badges`/`actions`/`quiet` added — divergences in its header), drawn by `packages/ui/src/components/agents/page/Overview.tsx`, `HarnessPanel.tsx`, `BuiltinPanel.tsx` and `AgentEditor.tsx` (`AgentList.tsx` is the selectable list beside them and does not draw the card); the pure helpers are `packages/ui/src/agents/model.ts`. The `.pi/agents/*.md` reading of this row is gone: definitions are Laser's, in `<state>/agents.json` |
| Handoff | What a child agent tells its parent, shown inline in the parent's transcript — **adopted**: `elements/agent-handoff.tsx`. What is mounted is its `AgentEventCard`, drawn by `thread/AgentEventMessage.tsx` for every `lasercode/agent-event` custom message (D-140, M13-T6): the child's `subagentName` and `agentName`, the run state, `endedBy` with the person's verbatim reason when they ended it, the final message, and a link to the child session — the message is stored once in the child and this card shows the child's updated state to the parent, never a second copy of the transcript. The original `AgentHandoff` strip (parent → child with what came along) is not mounted; its export was removed from the file (M13-T61) — it had no caller since the run island went (D-147), and the map's edges (`agents/map/`) say the same thing structurally. If a parent → child strip is ever wanted inline, re-install `elements-agent-handoff` beside `AgentEventCard` rather than growing the card |
| Background runs | **Not applicable as a second navigation list (D-103); the copied background-inbox component is deleted.** The canonical session row carries working/waiting/error/unread state. A separate attention inbox duplicated the same destination and is intentionally not mounted. Detached agent work remains in the fleet |
| Checkpoints | Pi's session tree as checkpoint history (M1-T9) — **adopted**: `elements/checkpoint-history.tsx` replaces `components/shell/HistoryTree.tsx` (deleted); the telemetry panel mounts it. A tree (depth, branch starts, labels), not a list; Restore became Jump and Fork, which are Pi's two ways back; the current entry is the live colour |
| Schedule | **Retired concept (D-140).** It claimed pi-subagents scheduled runs, deferred by D-19 Q4; the harness has no schedules and `AgentRunOrigin` is `agent` or `user` only. Not applicable until a Laser-owned schedule is a decision |

## Observability

| Element | laser surface |
| --- | --- |
| Trace waterfall | Provider request timing in the logs screen — **adopted**: `packages/ui/src/components/assistant-ui/elements/trace-waterfall.tsx`, the logs screen's **Timing** view over the loaded rows; `spansFromEntries()` (`logs/model.ts`) pairs `provider_request`→`provider_response` and `tool_start`→`tool_end` by `correlationId`, nests tools under the request they ran in, and leaves an unanswered start running. Picking a span selects its row. **No TTFT span**: Pi's `after_provider_response` gives a host status and headers, not the stream (R3) |
| Cost meter | Cost in the monitor — **adopted**: `packages/ui/src/components/assistant-ui/elements/cost-meter.tsx`, the API view of `shell/TelemetryPanel.tsx`: API-billed session total, last turn, per-model lines from `usageByModel()`, per turn. Account-authenticated turns never enter these totals. A child agent contributes its *model* and no numbers, because the harness never measured its tokens (`ux-fleet.md` R5) — enough to put the session in the mixed billing view, not enough to invent a figure. Numbers, not preformatted strings; "not measured" instead of `$0` |
| Quota banner | Account-provider allowance — **adopted**: `packages/ui/src/components/assistant-ui/elements/quota-banner.tsx`, fed by normalized `lasercode/account-usage/state` snapshots. Related periods share a bucket heading in `AccountAllowances.tsx`. Chat uses compact documented buckets; Settings → Usage retains every dynamic bucket and credits. Source-linked help explains known limits and qualifies unknowns. Countdown/exact local reset time is a shared host-stored preference. Account-only sessions replace spend; mixed sessions have Account/API tabs. Read-only refresh retains the last good snapshot (D-90, D-112) |

## Composer

| Element | laser surface |
| --- | --- |
| Composer | The composer — **adopted**: `packages/ui/src/components/assistant-ui/elements/composer.tsx` is the vocabulary (`ComposerBar`, `ComposerToolbar`, `ComposerAttachments`/`ComposerAttachmentTile`, `ComposerAttachButton`, `ComposerVoice`/`ComposerVoiceButton`, `ComposerSend`) and `thread/Composer.tsx` composes it over `ComposerPrimitive`. Nothing reserves a footer row below it; tooltips and Settings → Help and shortcuts own guidance. Removed from the registry copy, each with its reason in the file header: the `<input>` (the primitive is the textarea), the menu and its slash/mention filters (`composer-trigger-popover`), the model trigger (`model-selector`), the fake context ring (`context-display`) |
| Slash commands | `/` in the composer — **adopted** through `composer-trigger-popover` with `unstable_useSlashCommandAdapter`; today the popover offers laser's own commands (`/compact`, `/fork`, `/new`, `/clear-queue`, `/history`, `/project`). **closed**: the agent's extension commands, prompt templates and `/skill:` commands now arrive from `pi/commands/list` and are grouped under laser's own. The agent's *builtin* terminal commands are deliberately excluded — `/model`, `/settings`, `/tree` and `/thinking` open pickers in a terminal UI this app never runs, and laser has its own control for each, so a row that opened nothing would be worse than no row. Picking a row writes `/name ` into the composer ready for its arguments; the agent runs it on send, not on click. **Tab is completion, not selection (M15-T5)**: it writes the highlighted command's own word (no trailing space, the way a shell completes one), runs nothing and sends nothing, and leaves the picker open on the exact match — so choosing the row with Enter or the mouse, or sending the message, stays the person's explicit second act |
| Mentions | `@` in the composer — **adopted** through `composer-trigger-popover` with `unstable_useMentionAdapter`; the items are the open session's child agents by their `subagentName`, so `@auth-audit` addresses a child this session started. **closed**: `@` is now one flat list — subagent handles first, then project files from `pi/project/files`, which is `git ls-files` (tracked and untracked-but-not-ignored, so `.gitignore` is honoured by the tool that owns it) with a bounded walk for a directory that is not a repository. Untracked files carry "Not tracked by git yet" as a second line. Deliberately not the agent's own `find` tool: that downloads `fd` on first use, and a self-contained app does not fetch a binary to fill in a popover |
| Attachments | Image paste and attach — **adopted**: `ComposerAttachmentTile` in `packages/ui/src/components/assistant-ui/elements/composer.tsx` draws each `AttachmentPrimitive` (preview or type icon, name, size or failure) inside `ComposerPrimitive.Attachments`; paste still lands through the primitive input |
| Agents | The pre-turn ordinary-session picker — **adopted by composition**: `elements/agent-selector.tsx` reuses the searchable props-driven model-picker list, starts on the configured default custom agent, excludes built-in product channels, moves plain draft text across the empty-session identity switch, refuses attached drafts rather than stranding them, stays out of built-in/empty Chat channels, locks the composer until the switch settles, and unmounts after the first prompt (D-185) |
| Models | The model picker — **adopted**: see Model selector below; `SessionModelSelector` sits in the composer toolbar immediately after the pre-turn agent selector when it is present |
| Dictation | `pi-gpt-transcribe` (M8-T2) and the mobile mic (M7-T6) — **adopted**: `ComposerVoiceButton` (mic ↔ stop in one slot) and `ComposerVoice` (five bars from the *real* level, elapsed, pending phrases) in `packages/ui/src/components/assistant-ui/elements/composer.tsx`; `mobile/DictateButton.tsx` binds the runtime and inserts each ordered phrase at the live caret without blocking keyboard edits (R2) |
| Context | The composer's context ring — **adopted** through Context display (below); files, context files and quoted text attached to a turn are not exposed by Pi to a host today, so the ring is the honest content (R3) |
| Draft restore | An unsent composer draft, per session, across reloads — **adopted**: `packages/ui/src/components/assistant-ui/elements/draft-restore.tsx`; `useComposerDraft` saves the composer text per session path while it is non-empty and drops it on send, `ComposerDraftRestore` offers it back above the composer with Restore / Discard |
| Context breakdown | What is filling the context window. **Not installed in wave 3**: Pi hands a host `tokens / contextWindow`, not the split into system prompt, tools, files and conversation the element draws; segments that cannot be filled are R3's fake. Claimed for a `contextUsage.breakdown` on `SessionState` (protocol first) |
| Prompt library | The agent's prompt templates. **Closed as a group inside the `/` popover, not as a separate element**: `pi/prompts/list` exists and the templates arrive with `source: "prompt"` alongside skills and extension commands, which is where a person looks for them. A dedicated browser would be a second place to find the same six rows. Re-open this row only if a prompt grows something a popover row cannot show |
| Command palette | `Cmd+K` — **adopted**: `packages/ui/src/components/assistant-ui/elements/command-palette.tsx` (combobox with `aria-activedescendant`, arrows wrap, Enter runs), mounted by `shell/CommandPalette.tsx` in a dialog: this session's commands, the app's, every session and every project by name. Matching covers label, detail and group |

## Voice

| Element | laser surface |
| --- | --- |
| Voice conversation | **Not applicable in v1.** Dictation is input-only |
| Read aloud | **Deferred.** Plausible for a phone; not v1 |

## Thread

| Element | laser surface |
| --- | --- |
| Chat panel | The thread column. **Not installed**: it is a fixed chrome (header, title, avatar, its own composer) around a message list, and the thread column here is not a card — it carries the guardrail, the waiting notice, the question card and the status line above `ThreadPrimitive.Viewport`. Composed from primitives instead; see Thread |
| Empty state | The session empty state — **adopted**: `elements/empty-state.tsx` (Root, Eyebrow, Greeting, Description, Suggestions, Suggestion on `ThreadPrimitive.Suggestion`), left-aligned; `EmptyStateComposer` dropped because the real composer is on screen. `thread/EmptyState.tsx` feeds it the open session |
| Scroll anchor | Transcript scroll-to-bottom — **adopted**: `elements/scroll-anchor.tsx`, the floating "Jump to latest" pill on `ThreadPrimitive.ScrollToBottom` (hides itself at the bottom). The registry's demo viewport and its new-message count are gone: the runtime exposes no count and a guessed one would be a lie |
| Canvas (canvas-split) | **Not mounted since M13-T26; the file stays.** It was the dock's resizable pane and divider, and the dock went with the panels. What it offers — a resizable pane with a divider that has a real 44px grab on a coarse pointer — is exactly what a resizable side column will want, and the shell's three columns are fixed-width today. The registry's `CanvasSplit` row and `CanvasSplitThread` column are still **not** exported: laser's split row is the shell's own layout row and it holds five things, not two |
| Connection state | The reconnecting banner — **adopted**: `packages/ui/src/components/assistant-ui/elements/connection-state.tsx`; `HostConnectionState` derives online / dropped / reconnecting / resumed from the socket and holds "Back online" for a moment; a full-width line under the top bar, not a floating card. No attempt counter (the client does not expose one) and no `resumedTokens`. Replaces `shell/ConnectionBanner.tsx` (deleted) |
| Shared conversation | **Not applicable in v1.** No sharing; revisit with the relay |
| Search in conversation | **Adopted**: `conversation-search.tsx`, installed from `elements-conversation-search`, is mounted by `use-conversation-find.tsx` from Ctrl/Cmd+F and the top-bar magnifier. Live match counts, previous/next, exact excerpts and native CSS highlights use stable `data-message-id` roots. Selected reasoning/tools unfold transiently; closing restores the session's detail setting. The API request modal reuses this element with a section/full-request scope toolbar and separate native highlights; its diagnostic search includes JSON keys and values. Demo scrollbar markers were removed because folded message geometry cannot support their fake positions |
| Thread search | **Adopted**: `thread-search.tsx` powers inline sidebar search and the Ctrl/Cmd+Shift+F global dialog. A session appears once, with a project label and contained, highlighted excerpt identifying its source. User messages rank above assistant replies, then reasoning/tools; newest breaks ties. Host-owned saved-history search starts at 30 days with explicit older-range and result-page expansion; stale queries cannot replace newer results |
| Launcher | **Not applicable.** laser is not an embedded widget |
| Settings | **The settings screen** (M4-T2) — **adopted in part**: `packages/ui/src/components/assistant-ui/elements/settings-panel.tsx` keeps the element's switch and toggle row (`SettingsSwitch`, `SettingsToggleRow`), and every boolean setting in `settings/fields.tsx` is drawn with it. The card as a whole (model segmented control, system prompt, temperature) does not fit the Laser-owned two-scope settings taxonomy, so `SettingsForm` stays as the frame. General and permanent Advanced tabs receive only classified product settings; Full configuration expands secondary fields without exposing engine plumbing. Features are curated cards, not packages or extensions |
| Onboarding | First run — **adopted as the grammar, not the file** (M10-T6): `components/onboarding/SetupCard.tsx` frames every step of `components/onboarding/FirstRunFlow.tsx` — paper card, `n of N` eyebrow, dots that are buttons, Skip on the left — and adds the slot the element lacks, a body that is a form. The last step is one more `SetupCard` whose action starts the first session, so `packages/ui/src/components/assistant-ui/elements/onboarding.tsx` itself is **not mounted; file removed (M13-T61)** — it had no importer, `SetupCard` follows its grammar rather than importing it, and `SetupCard`'s header names the registry element as the reference. `elements-onboarding` is the thing to re-install if a step ever needs the element's own frame instead of `SetupCard`. `shell/FirstRun.tsx` is deleted. "No auth, no model" are no longer deferred to Settings — they are steps 2 and 3, because a person who has just installed the app has neither, and sending them to a settings screen to find out is the failure this row used to describe. Completion is host state (`pi/setup/state`), so the flow resumes at the first thing actually missing rather than at the step this browser last saw |
| Mobile composer | **The phone composer** (M7-T2) — **adopted**: `packages/ui/src/components/assistant-ui/elements/mobile-composer.tsx` rebuilt on the primitives: a pill between two 44px controls, the mic inside the pill, the same `ComposerPrimitive.Input` (16px on touch, Enter = newline). The registry's `<input>`, quick-action chips, drag handle and "return to send" are gone with their reasons in the file. Keyboard handling stays in the thread footer: `--kb` from `visualViewport` combined with the safe area through `max()` |

## AUI-connected

| Element | laser surface |
| --- | --- |
| Thread | The thread — **composed from primitives, on purpose**: `components/thread/Thread.tsx` builds the column out of `ThreadPrimitive.Root` / `Viewport` / `ViewportFooter` / `Messages` rather than installing the registry `thread.aui`. The registry element is a finished surface (its own composer, its own welcome, its own action bars) and this column has to carry things it has no slots for: the guardrail, the waiting notice and the question card above the composer, the status line, and a composer with laser's own three-key contract. Every *part* it would have given us is installed and mounted separately (`message-pair`, `message-actions`, `message-branches`, `composer`, `empty-state`, `scroll-anchor`, `message-timestamp`), which is the same content without a chrome that fights the shell. The questions above the composer (`ThreadDialogCards`, `WaitingNotice` from `dialogs/`) and the goal bar (`thread/GoalBar.tsx`) are the column's own |
| Assistant modal | **Not applicable.** laser is the app, not a modal in one |
| Assistant sidebar | **Not applicable.** Same reason |
| Thread list | The sessions panel — **adopted**: `packages/ui/src/components/assistant-ui/elements/thread-list.aui.tsx` on `ThreadListPrimitive` / `ThreadListItemPrimitive` / `ThreadListItemMorePrimitive`: quiet project folders from `custom.cwd`, newest-first single-line rounded rows, Pin / Rename / Archive / Copy path, and Archived with Unarchive / Delete. Client-local pins move their canonical row to Pinned with a 12px project label; filtering is shared with the rail. **One activity indicator per row, before the name, and it is that session's own state (M15-T4)**: `SessionActivity` draws the shared `StatusDot` — the sweep for working, the pulse for a question, static marks for unread and error, nothing for idle — and a child row uses the equivalent accessible run-state dot for a live run, falling back to its run's terminal mark once the session itself has nothing to say. There is no trailing spinner and no second mark at the row's end. A parent never wears a descendant's *work*: `live` and `muted` are gone from `branchTone`, because work that is going is visible on the rows doing it. A **folded** branch still marks the two states that would otherwise have nowhere to appear (D-186): a hidden **question**, pulsing, and a hidden **failure**, static — a failed run sits inside the finished fold inside a settled branch, two closed folds deep, so the mark is its only visual trace, and stillness keeps it from reading as live work. A failed child's own row keeps the danger mark even while the session is unread. The disclosure's accessible name and tooltip keep the complete counts, so no status tag steals title width. Finished counts remain on the finished-work fold. Project rings and the duplicate inbox do not repeat that activity (D-103). `shell/SessionsPanel.tsx` owns only the frame. `useRemoteThreadListRuntime` and the catalog adapter retain selection and lazy hydration; local pin/archive preferences do not alter the engine transcript. In the Chat tab the row menu adds “Move to a project…”, which opens the shell's `MoveSessionDialog` (M13-T58): the session keeps its history, name and id and moves into a chosen or newly picked project |
| Thread list sidebar | The sessions panel's sidebar form (D-20). **Not installed**: `threadlist-sidebar` is a shadcn `Sidebar` shell (provider, collapsible rail, demo header and footer links) around `ThreadList`; the app's shell already owns that frame — a 288px column toggled with `[` on desktop, a sheet below — and a second sidebar system would fight it. The project groups it was claimed for live in Thread list |
| Orb | **Not applicable; the copied files are deleted** (`elements/voice.tsx`, `elements/voice.aui.tsx`). A WebGL orb is a picture of activity with no measurement behind it, and every surface floated for it already shows something truer: the fleet pill and the ambient line use `StatusDot` (one status vocabulary, R1), and dictation — the only place the app has a real signal — draws `ComposerVoice`'s five bars from the actual microphone level. A 16px canvas per pill is also a real cost for a decoration. Nothing imported either file |
| Reasoning | Thinking blocks — **adopted as parts**: `elements/reasoning.tsx` (Root, Trigger, Content, Text, Fade) is what draws them, through `ActivityReasoning` in `elements/tool-group.aui.tsx` — the reasoning row *is* an activity row (D-89, D-96), mounted by `thread/messages.tsx`. The runtime-bound `elements/reasoning.aui.tsx` (`ReasoningGroup`, the standalone "Reasoning · 3.2s" collapsible) is **not mounted; file removed (M13-T61)** — it had no importer: the group disclosure replaced it, and two reasoning rows with different grammars is the "assembled" look this inventory exists to prevent. The installed thinking indicator owns the empty running-reply state and the shared activity row's live thought state. Adjacent reasoning and tools share the Tool group parent (D-89); opening it renders the complete reasoning Markdown through `ReasoningText`. The session's three-level Activity detail preference decides whether the parent and its action bodies start open, while errors and decisions remain visible. The composer's reasoning-effort wrapper also works before a project session exists: it derives the configured default agent/model's supported levels, then creates or reuses an ordinary empty session before applying the session-only override (D-185). Draft text moves with that transition, and input/Send stay locked until the level is applied; project-model changes invalidate its capability view. Pi stamps no reasoning duration, so aggregate live time is our wall clock (`thread/timing.ts`) |
| Message timing | Per-turn elapsed, generated token count and throughput — **adopted**: `elements/message-timing.aui.tsx`, in the reply's footer once it settles. Reads `useMessageTiming()` first, then `metadata.custom.laser.timing` / `.usage`, then our wall clock for a turn this window watched. The compact footer shows duration, output tokens and `tok/s`; measured runtime throughput wins, otherwise it derives the rate from the same visible output and duration. Renders nothing rather than inventing zeros |
| Conversation map | Navigating a long transcript; pairs with the history tree — **adopted**: `elements/conversation-map.aui.tsx` + `elements/conversation-map.tsx`, a sticky rail at the viewport's edge from four turns up, on `lg` widths. The preview card is Radix `HoverCard` rather than Base UI's `PreviewCard` (one dependency fewer) |
| Context display | The context ring — **adopted**: `packages/ui/src/components/assistant-ui/elements/context-display.tsx`, the props-driven core (`context-display.aui` reads AI SDK usage through `@assistant-ui/ai-sdk`, which the app does not use). `ContextRingButton` binds it to `contextUsage` in the composer toolbar, top bar and telemetry rail; clicking opens one detailed icon-led inspector with real used/remaining/capacity/percentage values, window-health guidance and current model, while the rail keeps the 64px glanceable form. Tone thresholds are DESIGN.md's (`toneForPercent`); the per-kind token split is gone because Pi gives one total. `StatusRing` no longer has a `percent` path and is purely the status arc |
| MCP config dialog | MCP server configuration, where a project uses one. **Not installed in wave 3**: `mcp-config` is `@assistant-ui/react-mcp`'s *browser-side* MCP manager (servers the page itself connects to, OAuth in the tab), which is the wrong model here — Pi runs MCP through extensions inside the worker, configured in settings. `mcp-server-panel` (the props-driven list) is the right shape, but no protocol method reports a project's MCP servers yet; protocol first, then that panel in Settings → Packages |
| Attachment | Attachment chips — **adopted**: `elements/attachment.aui.tsx` (`UserMessageAttachments`, `ComposerAttachments`, `ComposerAddAttachment`) on our Radix `Tooltip` / `Dialog`, no `Avatar`, no `zustand` (`hooks/use-attachment-src.ts` is two primitive selectors). Wired on the sent message (`thread/messages.tsx`); the composer side is the `composer` element's tile (row above) |
| Follow-up suggestions | Suggestions in the empty state and after a turn settles — **adopted**: `elements/follow-up-suggestions.aui.tsx`, the row above the composer once a turn settles, fed by a thread-scoped `AuiConfig({ suggestions: Suggestions([...]) })` in `thread/Thread.tsx` (run the tests, show the diff, commit — static on purpose: Pi produces none, and an invented per-turn set is a guess). The empty state uses `ThreadPrimitive.Suggestion` through `empty-state` |
| Tool fallback | An unknown tool's row — **adopted**: `packages/ui/src/components/assistant-ui/elements/tool-fallback.aui.tsx`; `ToolFallback` draws any tool Pi does not ship, and its parts (Root, Trigger, Content, Args, Result, Error, Approval) are what every tool row is composed from. Approval keeps the catalog's option kinds and adds `denyFeedback`, so Deny opens a reason field whose text reaches the agent as `Denied: …` (D-20). **Enter never allows**: the catalog autofocuses the first allow option and the bare "Allow"; both moved to the reject option, or to Deny when the payload declares none (see Permission grant) |
| Tool group | **Collapsed reasoning and tool activity** (D-20 item 4, D-89) — this element is that feature. **Adopted**: `packages/ui/src/components/assistant-ui/elements/tool-group.aui.tsx`; `ToolGroup` takes the chronological `group-activity` part from `MessagePrimitive.GroupedParts`, including a single call, renders a deliberately muted compact surface, and reports “Reasoned” beside counted action families when settled. While live, the thinking-indicator element names either active thought or the exact active call. Opening it preserves complete reasoning Markdown and every catalog child row, with scroll lock and staggered reveal. Answers only keeps the parent shut, Show reasoning opens only parents containing reasoning, and Show everything also opens every action body. One design, no `variant` cva. Replaces `thread/ToolGroup.tsx` (deleted) |
| Quote | Quote selected transcript text into the composer — **adopted**: `elements/quote.aui.tsx` (`SelectionToolbar` over the thread, `ComposerQuotePreview` in the composer card, `QuoteBlock`). Pi's prompt is plain text, so the composer folds the quote in as a markdown blockquote at send (`quoteAsMarkdown`, `thread/Composer.tsx`) and `quote-reply` renders it back on the sent message |
| Sources | web-access sources under an answer — **adopted**: `packages/ui/src/components/assistant-ui/elements/sources.aui.tsx`, registered for `source` parts in `thread/messages.tsx`; the eight tinted variants are one chip, and the favicon fetch is opt-in (off by default: fetching an icon per domain tells a third party what was read). Pi emits no `source` parts yet, so it draws nothing today |
| Image | Image message parts — **adopted**: `elements/image.tsx`, tokens mapped, the spinner replaced by the working dot; registered for `image` parts in `thread/messages.tsx`. Today the projection carries only an image count per prompt (chips via `message-attachment`), so the renderer is ready for the day it carries the part |
| File | File message parts — **adopted**: `elements/file.tsx`, tokens mapped; registered for `file` parts in `thread/messages.tsx` |
| Model selector | The model picker — **adopted**: `packages/ui/src/components/assistant-ui/elements/model-selector.tsx`, the props-driven core (`elements-model-selector`, the inventory's "model-picker"); `SessionModelSelector` loads `pi/model/list` when the popover opens, groups by provider with the provider's mark, opens scoped to the active session provider with its model marked, and a pick calls `pi/model/set`. Temporary search and provider filters reset on close. The `.aui` wrapper was **not** installed: its one addition registers the pick with assistant-ui's ModelContext, which this runtime never reads. The effort section is gone (Reasoning effort owns the level). `cmdk` was added for it. Replaces `thread/ModelSelector.tsx` (deleted) |
| Composer trigger popover | The slash-command and mention popovers — **adopted**: `packages/ui/src/components/assistant-ui/elements/composer-trigger-popover.aui.tsx`; mounted twice under one `Unstable_TriggerPopoverRoot` (`/` action, `@` directive). M12-T62 adds scoped selected-row scrolling, PageUp/PageDown, outside/focus/Escape dismissal, composition-safe input ownership, viewport-aware layout and explicit result states. M15-T5 adds `onComplete`: where a picker's rows *do* something, Tab has to complete rather than select — the primitive's keyboard resource treats Tab exactly like Enter — so the element writes the returned draft and caret itself and the footer reads “Tab complete · ↵ select”. `@` queries the project index per debounced query, not a locally filtered first page; stale replies are discarded. Hover/keyboard selection exposes skill/prompt source details, with a separate OS-associated Markdown open action (Alt+O) or browser/phone copy-path fallback. No second keyboard/search widget or new dependency. |
| Directive text | System and directive messages, and extension notices — **adopted**: `elements/directive-text.aui.tsx` + `elements/directive-text.tsx`; `@file` / `@handle` directives render as `Badge` chips, never HTML. Wired for prompt text and notice text (`thread/messages.tsx`, `DirectiveString`) |

### Composer and compact-disclosure refinements (D-183)

- Opening Beam prepares a real empty session immediately; the microphone uses
  that session's capability and path. Sidebar `+` reuses it in the main view.
- The thinking trigger shows its current short label beside the brain at every
  width; a model with no reasoning shows a noninteractive `off` explanation.
- Recording owns a full control row while active. Stop flushes pending phrases;
  the adjacent neutral × discards untranscribed audio, aborts pending requests,
  and prevents any waiting send from continuing. Already inserted/typed text
  remains editable. Recording has no duration cutoff; 20-second phrase cutting
  bounds individual uploads, not the lifetime of microphone capture. Device or
  audio-context interruption is visible with a next action.
- Skill rows use a single clamped description preview. Results get the available
  height inside their clipping surface (including Beam), while **Read full
  description** opens a separately scrollable view. Escape returns to results;
  opening/reading details never selects or runs the skill.
- Settled mixed-activity rows reserve a complete status line, then two readable
  family labels and `+N types` on a wrapping secondary line. Expanding reveals
  all individual actions; the accessible name retains every family. No series
  of mutually squeezed ellipses, duplicate per-family details, or smaller type.

### Activity-row interaction contract (D-96)

Tool group, Tool fallback and individual reasoning share `activityRow` and
`activityTrigger` from `surfaces.tsx`: muted surface, secondary ink, leading icon
and trailing chevron. `ActivityReasoning` uses the Tool group disclosure parts
around the complete `ReasoningText`, instead of the old static reasoning section.
An aggregate is added only for multiple actions; its count matches the visible
child rows. Show reasoning keeps this parent closed and opens only reasoning
inside after the reader expands it (superseding the earlier parent-open wording
in the inventory). Every row is manually reversible, including waiting tool
details; approval controls stay outside their fold. `activity-disclosure.test.tsx`
tests the actual buttons, mounted bodies, live updates and independent children.

### Captured request inspector (D-97)

The user-message actions element and log detail open one large dialog. Adopted
SpecSheet and JsonViewer elements render capture metadata and complete JSON;
the shared disclosure rows render instructions, conversation, tools and provider
parameters. Unknown fields remain inspectable. Exact prompt links are captured
at the engine hook; timestamp-only legacy attribution is visibly qualified.
No request is recreated or sent by the inspector. Credential redaction remains
in the host before persistence; retention and summary-only gaps are explicit.

## Renderers

The Connection state element also owns release notices: a compiled-version
handshake blocks an incompatible frontend before session hydration. A remote
Refresh view action only reloads the frontend and explicitly keeps agents and
sessions running. Native update notices offer a person-chosen full app/host
restart and warn that active work stops. No update is applied automatically.

| Element | laser surface |
| --- | --- |
| Markdown text | The transcript renderer — **adopted**: `packages/ui/src/components/assistant-ui/elements/markdown-text.tsx`, the primitive plus our token-mapped component map, compact 14px prose and 8px block rhythm, remark-gfm and **no rehype-raw** (invariant 9), math via remark-math/rehype-katex, fences through the Shiki element, `mermaid` fences through the Mermaid element, footnote refs as citation chips, and a streaming caret on the last text block after text begins. Before any visible part arrives, the message surface owns the full thinking indicator instead. `dot.css` is not imported. Replaces `thread/MarkdownText.tsx` (deleted); `preview/MarkdownPreview.tsx` draws through it. The captured-request inspector reuses this exact component under `TextMessagePartProvider` for its optional Markdown view, so instructions and conversation content retain chat typography, spacing, code treatment and safety rather than creating a second renderer |
| Syntax highlighter | Code blocks — **not installed**: Shiki won the evaluation (next row), so the Prism fallback would be a second highlighter nobody renders. `highlighter.tsx` (hljs) is deleted and `react-syntax-highlighter` removed from the package |
| Shiki highlighter | **Evaluated and adopted**: `packages/ui/src/components/assistant-ui/elements/shiki-highlighter.aui.tsx` + `shiki-highlighter.tsx`. Streaming fences stay plain; settling is a colour change in the same box. Settled fences use the full 242-language Shiki catalog, its official aliases, model-label normalization and the Oniguruma engine for complete TextMate-regex compatibility; every grammar and the engine remain lazy. The theme is laser's own: broad language scopes map only to `--syntax-*` and status tokens, one theme for both bases (T1). The former hljs renderer (`thread/highlighter.tsx`) is deleted |
| Mermaid diagram | Mermaid fences — **adopted**: `packages/ui/src/components/assistant-ui/elements/mermaid-diagram.aui.tsx` + `mermaid-diagram.tsx` (`beautiful-mermaid`). Divergence that matters: the renderer copies node labels into the SVG verbatim, so the output goes through `utils/svg-sanitize.ts` (allow-listed elements, no `on*`, no external `href`, no `@import`/`url(` — tested) before it touches the DOM (invariant 9). Colours are tokens, the zoom overlay is the app's sheet vocabulary |
| Generative UI | **Evaluated — recommendation: do not adopt.** `JSONGenerativeUI` (`@assistant-ui/react-generative-ui`) is a *model-driven* `present({ display })` tool: the LLM composes a tree from a component vocabulary (`$type`, children), rendered by a registry with an allowlist. laser has no such vocabulary and does not want one: since M13-T26 an extension shows what it has through the tool call that produced it, and adopting this would put the general-purpose display bus back — the exact thing the fleet replaced, with the additional problem that the composer would be a language model. Where it *could* fit: a component tree from a *trusted* source rendered through `GenerativeUIRender` with our token-mapped registry. Nothing produces one |

## Primitives

| Element | laser surface |
| --- | --- |
| Tooltip icon button | Every icon button in the app — **verified in wave 3** across lane D's surfaces: `components/ui/tooltip-icon-button.tsx` is the restyled copy (it adds `shortcut` and the app's sizes, and drops the per-button `TooltipProvider`); the icon-only `Button`s that had slipped past it (sessions row menu, settings reload, notice dismiss, and the workbench's Close, which had also drifted into two names — tooltip "Back to the session", `aria-label` "Close") now go through it. That drift is the reason for the element: `TooltipIconButton` derives the accessible name from the tooltip, so the two cannot disagree |
| Model logos | Provider logos in the model picker and telemetry rail — **adopted**: `packages/ui/src/components/assistant-ui/elements/logos.tsx`; every mark renders in `currentColor` (the registry's brand hexes and gradient are gone, T1), and `ProviderLogo` maps Pi provider ids onto the marks with a monogram fallback. The rail uses the same mark as the picker so provider identity can be recognized before reading the model id |
| Heat graph | Session activity per project; pairs with Activity graph. **Not installed in wave 3**, for that row's reason exactly: nothing renders per-project history yet, so the file would have no importer. It lands with Activity graph, on a project header or Settings → Projects |

## Generative demos

The `generative-*` set (stays, booking, flights, weather, receipts, charts)
are **domain demos, not applicable**. Their value to us is the pattern, not
the components: they show a model composing UI from a fixed vocabulary. laser
has no such vocabulary and does not want one — an extension shows what it has
through the tool call that produced it (`docs/ux-fleet.md`).


## Beyond the published catalog

The published elements page lists ~121 items. The registry source has **125
element files**, and twelve of them are not on that page. They were found by
listing
`packages/ui/src/components/react/assistant-ui/elements/` in the assistant-ui
checkout, which is the authoritative list. Each is claimed here too.

| Element | Exports | laser surface |
| --- | --- | --- |
| `surfaces` | `paper`, `floating`, `field` | **The shared style vocabulary every other element builds on.** **Adopted** at `packages/ui/src/components/assistant-ui/elements/surfaces.tsx`, mapped onto the semantic tokens: `paper` is `bg-surface border-line`, `mono` is `typed` (12px, the floor), `live` is `text-live`, every duration a motion token. `utils/range.ts` (`elements-range`) came with it. `fieldInteractive` and `inkButton` came with the copy and never found a caller — deleted, on the same standard as any other unmountable code |
| `shimmer-labels` | shimmer helpers | The label that shimmers while a run streams — **adopted** as `ShimmerLabel` / `SwapLabel` in `surfaces.tsx` (the registry has no separate file; `shimmer-labels.test.tsx` tests those). The reasoning header now shimmers through `ShimmerLabel` (via `thinking-indicator`) rather than a class spelled in `messages.tsx`; the `shimmer-text` utility in `globals.css` stays as the token-driven implementation behind it, so `tw-shimmer` is not imported |
| `reasoning-panel` | `ReasoningPanel` | The expanded reasoning body, distinct from the `reasoning` collapsible header — **not mounted; file removed (M13-T61)**: `elements/reasoning-panel.tsx` was de-demoed from a titled-steps collapsible to the body only (Pi's reasoning is one markdown stream), the hairlined, bottom-pinned, fading scroller under `reasoning.aui`'s trigger — and `reasoning.aui` had no importer and is removed too (see Reasoning). The activity row draws `ReasoningText` inside the Tool group's own content instead; a standalone reasoning body would re-install `elements-reasoning-panel` |
| `suggestions` | `Suggestions` | Empty-state and post-turn suggestions, the props-driven sibling of `follow-up-suggestions`. **Not installed**: `follow-up-suggestions.aui` is the one kept (it reads `thread.suggestions`, which the thread configures), and the empty state composes `ThreadPrimitive.Suggestion` itself. Two pill components for one job would be the "assembled" look this inventory exists to prevent |
| `quote-reply` | `QuoteReply` | The quoted block shown on the message you are replying to, paired with `quote` in the composer — **adopted**: `elements/quote-reply.tsx`, the quoted block plus `splitLeadingQuote`, which lifts the blockquote the composer folded into a prompt back out for display (`thread/messages.tsx`). The demo's highlighted selection and toolbar are `quote`'s |
| `message-attachment` | `MessageAttachments` | Attachments on a sent message, distinct from `attachment` in the composer — **adopted**: `elements/message-attachment.tsx` as chips (kind, name, optional detail); the transcript projection keeps only an image count, so there is no thumbnail or size to fake. Replaces the "2 images" `Badge` on the prompt bubble |
| `model-picker` | `ModelPicker` | The props-driven picker — **kept** (installed as `elements-model-selector` → `model-selector.tsx`); the runtime-bound `model-selector.aui` was not installed. See Model selector |
| `threadlist-sidebar` | `ThreadListSidebar` | The real filename behind "Thread list sidebar". **Not installed**; see Thread list sidebar |
| `voice` | `VoiceOrb`, `VoiceControl`, `VoiceStatusDot`, `deriveVoiceOrbState` | **Not installed** (the wave-3 copies were deleted; see "Orb"). `VoiceControl`'s connect / mute / disconnect are out of scope for v1 and a control that does nothing is a lie (R2). The dictation state dot the reusable half was wanted for is `ComposerVoice`'s own `StatusDot`, driven by the `phase` prop its caller already holds — a props-driven element reading the runtime itself would break that contract for no gain |
| `flow` | `Flow`, `FlowLLM` | A declared workflow graph; `FlowLLM` renders an LLM step. **Not in the registry** (`r.assistant-ui.com/styles/new-york/flow.json` and `elements-flow.json` both 404 on 2026-09-05): the file exists in the assistant-ui checkout but is not published, so it cannot be installed. Nothing draws a declared graph since the plan panels went (M13-T26, D-147); revisit only if a producer returns. The 404 is moot for the agents live map (D-140): that surface is Laser-owned on `@xyflow/react` under `packages/ui/src/agents/` and `packages/ui/src/components/agents/`, not a catalog element |
| `flow-canvas` | `FlowCanvas` | A pan/zoom surface for a declared workflow graph. **Not in the registry** (404, see `flow`), and no surface needs it: the plan islands are gone, and the agents live map brings its own React Flow canvas (`packages/ui/src/components/agents/map/`) |
| `flow-expand` | `FlowExpand` | Expanding a node in that graph. **Not in the registry** (404, see `flow`); the live map's node opens its inspector (`agents/map/Inspector.tsx`) and its chat instead |

Eight names on the published page have no file of their own because they are
**sub-features composed inside another element**, not separate installs:
`composer-slash-commands`, `composer-mentions`, `composer-attachments`,
`composer-model-picker`, `composer-voice` and `composer-context` all live
inside `composer`; `orb` lives inside `voice`; `thread-list-sidebar` is
`threadlist-sidebar`. Install the parent and configure the feature.

## How this interacts with the fleet

[`docs/ux-fleet.md`](ux-fleet.md) says *where* something renders and *who
decides*. This inventory says *what draws it*. The fleet is a column beside
the monitor (a sheet below 1280px) and its list is Subagent list, scoped to
the open session's tree (D-160); a question is Approval card, Permission
grant or Elicitation form, chosen by `DialogBody` and answered inline in the
transcript — in its tool row or above the composer, never in a column
(D-147); there is no inbox (D-103), the session row carries attention. The
fleet document is the frame, the elements are the picture. The tool-use rows
were swept element by element in
[`ux-elements-audit.md`](ux-elements-audit.md).
